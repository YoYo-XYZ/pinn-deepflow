{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"DeepFlow: Physics-Informed Neural Networks for Fluid Dynamics","text":"<p>DeepFlow is a user-friendly framework for solving PDEs, with a focus on fluid dynamics including the Navier\u2013Stokes equations, using Physics-Informed Neural Networks (PINNs). It provides a CFD-solver-style workflow that makes PINN-based simulations accessible and straightforward.</p>"},{"location":"#getting-started","title":"Getting started","text":"<ul> <li>Quick Start \u2014 solve steady channel flow in under 20 lines of code</li> <li>Installation \u2014 pip, the <code>[cfd]</code> extra, and GPU setup</li> <li>Examples \u2014 a gallery of 6 worked cases, from Burgers' equation to transient channel flow</li> <li>API reference \u2014 full documentation of the <code>deepflow</code> package</li> <li>GitHub repository \u2014 source, long-form README, and issue tracker</li> </ul>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>\u27c1 Physics-Attached Geometry: AUTO GENERATE TRAINING DATA by explicitly attach physics and neural network to geometries.</li> <li>\ud83d\udd27 CFD-Solver Style: Straightforward workflow similar to CFD software.</li> <li>\ud83d\udcca Built-in Visualization: Tools to evaluate and plot results.</li> <li>\ud83d\ude80 GPU Acceleration: Enable GPU for faster training.</li> <li>\ud83d\udd22 FP64 Precision: Switch to double precision for improved PINN accuracy/stability.</li> <li>Flexible Domain Definition: Easily define complex 2D geometries.</li> </ul>"},{"location":"#current-implementations","title":"Current Implementations","text":"<ul> <li>Supported problems: solving forward partial differential equations (PDEs)<ul> <li>transient &amp; steady 2D incompressible Navier-Stokes equations, 2D Fourier Heat equation, Burgers' equation</li> </ul> </li> <li>Sampling methods: Uniform, Random, Latin Hypercube Sampling, RAR-G [0], R3 [1]</li> <li>2D Geometries: Custom functions, Rectangle, Circle, Polygon, and combinations &amp; subtractions.</li> <li>Hard Boundary Conditions: Automatic Hard BC w.r.t. to geometry.</li> <li>Neural Network Architectures: Fully connected feedforward networks (FNN).</li> <li>Optimizers: Adam, L-BFGS</li> <li>Backend: PyTorch, with an optional NGSolve FEM reference backend (<code>pip install \"deepflow[cfd]\"</code>)</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the MIT License - see the LICENSE file for details.</p>"},{"location":"BFS_flow_steady/","title":"Solving the Backward-Facing Step Flow (Steady)","text":"<p>This example solves steady flow over a backward-facing step \u2014 a standard benchmark for separated flows. After the step, the flow detaches and forms a recirculation zone whose length is a classic validation metric.</p> <p>At a glance</p> <ul> <li>Physics: steady incompressible Navier\u2013Stokes equations,   nondimensionalized with <code>(U, L) = (0.0001, 1)</code> and <code>mu = 0.001</code>,   <code>rho = 1000</code> \u2014 Reynolds number <code>Re = \u03c1UL/\u03bc = 100</code>. Parabolic inflow   on the upper inlet (<code>y \u2208 [0.6, 1]</code>), no-slip walls (including the step   faces), <code>p = 0</code> at the outlet.</li> <li>What to expect: the boundary layer separates at the step corner and   reattaches further downstream, enclosing a recirculation zone. The   reattachment length is the number to verify against the literature.</li> <li>Setup cost: FNN 50 \u00d7 5 (outputs <code>u, v, p</code>); Adam 2,000 epochs   (lr 0.001) followed by L-BFGS 350 epochs (threshold 1e-4). Reference   run: a few minutes on GPU; L-BFGS reached <code>total_loss \u2248 2.5e-3</code> when   its epoch budget ran out.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"BFS_flow_steady/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>A six-vertex polygon describes the channel with the step:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\nrectangle = df.geometry.polygon(\n    [0, 0.4], [0, 1], [5, 1], [5, 0], [1, 0], [1, 0.4]\n)\n\ndomain = df.domain(rectangle)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"BFS_flow_steady/#2-define-the-physics","title":"2. Define the physics","text":"<pre><code>domain.bound_list[0].define_bc({'u': 0, 'v': 0})                          # Step face\ndomain.bound_list[1].define_bc({'u': df.parabolic_func('y', 0.6, 1, 0.7), 'v': 0})  # Inlet\ndomain.bound_list[2].define_bc({'u': 0, 'v': 0})                          # Top wall\ndomain.bound_list[3].define_bc({'p': 0})                                  # Outlet\ndomain.bound_list[4].define_bc({'u': 0, 'v': 0})                          # Bottom wall\ndomain.bound_list[5].define_bc({'u': 0, 'v': 0})                          # Step riser\ndomain.area_list[0].define_pde(df.NavierStokes(U=0.0001, L=1, mu=0.001, rho=1000))\ndomain.show_setup()\n</code></pre>"},{"location":"BFS_flow_steady/#3-sample-training-data","title":"3. Sample training data","text":"<pre><code>domain.sampling_lhs(bound_sampling_res=[1000, 1000, 1000, 1000, 1000, 1000], area_sampling_res=[2000])\ndomain.show_coordinates(display_physics=False)\n</code></pre>"},{"location":"BFS_flow_steady/#4-train-the-model","title":"4. Train the model","text":"<pre><code>def do_in_adam(epoch, model):\n    if epoch % 100 == 0 and epoch &gt; 0:\n        domain.sampling_R3(bound_sampling_res=[1000, 1000, 1000, 1000, 1], area_sampling_res=[2000])\n        print(domain)\n\nmodel0 = df.PINN(width=50, length=5, input_vars=['x', 'y'], output_vars=['u', 'v', 'p'])\n\nmodel1, model1_best = model0.train_adam(\n    learning_rate=0.001,\n    epochs=2000,\n    calc_loss=df.calc_loss_simple(domain),\n    threshold_loss=0.005,\n    do_between_epochs=do_in_adam)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=350,\n    threshold_loss=0.0001)\n</code></pre> <p>Reference-run result</p> <p>Adam (2,000 epochs) reduced <code>total_loss</code> from \u2248 55 to \u2248 0.5; L-BFGS (350 epochs) reached <code>total_loss \u2248 2.5e-3</code> \u2014 its epoch budget was exhausted before the 1e-4 threshold. Increase <code>epochs</code> in <code>train_lbfgs</code> or lower <code>learning_rate</code> in Adam to push further.</p> <pre><code>domain.show_coordinates()\n</code></pre> <p></p> <p>Save (and reload) the trained model:</p> <pre><code>model2.save_as_pickle(\"model.pkl\")\nmodel2 = df.load_from_pickle(\"model.pkl\")\n</code></pre>"},{"location":"BFS_flow_steady/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<pre><code>df.Visualizer.refwidth_default = 7\n\narea_eval = domain.area_list[0].evaluate(model2)\narea_eval.sampling_area([300, 60])\nprint(area_eval)\n\narea_eval['v_mag'] = (area_eval['u']**2 + area_eval['v']**2)**0.5\n\n_ = area_eval.plot_color('v_mag', s=2, cmap='jet').savefig(\"colorplot_v_mag.png\")\n_ = area_eval.plot_color('u', s=2, cmap='rainbow')\n_ = area_eval.plot_color('v', s=2, cmap='rainbow')\n_ = area_eval.plot_color('p', s=2, cmap='rainbow')\n_ = area_eval.plot_streamline('u', 'v', cmap='jet')\n_ = area_eval.plot_color('pde_residual', s=2, cmap='viridis')\n</code></pre> <p>The streamlines reveal the recirculation zone behind the step \u2014 the hallmark of this benchmark. Residuals concentrate at the step corner and along the reattaching shear layer.</p> <pre><code>_ = area_eval.plot_loss_curve(log_scale=True)\n</code></pre> <p></p>"},{"location":"BFS_flow_steady/#export-data","title":"Export data","text":"<pre><code>import numpy as np\n\nx_data = area_eval.data_dict['x']\ny_data = area_eval.data_dict['y']\nu_data = area_eval.data_dict['u']\n\narray = np.column_stack((x_data, y_data, u_data))\nnp.savetxt('outlet_velocity.txt', array)\n</code></pre>"},{"location":"BFS_flow_steady/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    fem_reference = domain.solve_fem(\n        mesh_size=0.2,\n        boundary_resolution=32,\n        max_iterations=20,\n    )\n    fem_area = fem_reference.area_list[0]\n    fem_area.sampling_area([100, 40])\n    fem_values = fem_area['u_ref']\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_values)))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error: {u_error:.6e}')\n</code></pre>"},{"location":"BFS_flow_steady/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/BFS_flow_steady/BFS_flow_steady.ipynb</code></p> <pre><code>jupyter notebook examples/BFS_flow_steady/BFS_flow_steady.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"burgers_eq/","title":"Solving the 1D Burgers Equation","text":"<p>This example solves the one-dimensional Burgers equation in its steady 2D embedding \u2014 the equation DeepFlow models as <code>BurgersEquation1D</code>. It is a good first example: the geometry is trivial, but the solution develops a sharp shock-like front that makes it a stress test for point sampling.</p> <p>At a glance</p> <ul> <li>Physics: <code>u_y + u \u00b7 u_x = \u03bd \u00b7 u_xx</code> with <code>\u03bd = 0.01/\u03c0</code> on the domain   <code>x \u2208 [-1, 1]</code>, <code>y \u2208 [0, 1]</code>, where <code>y</code> plays the role of time. Initial   condition <code>u(x, 0) = -sin(\u03c0x)</code>, homogeneous Dirichlet conditions   <code>u = 0</code> at <code>x = \u00b11</code>.</li> <li>What to expect: the smooth <code>-sin(\u03c0x)</code> profile steepens into a shock   front as <code>y</code> grows, until it is absorbed by the <code>u = 0</code> boundary at   <code>x = 1</code>. R3 resampling concentrates collocation points at the front.</li> <li>Setup cost: FNN 16 \u00d7 4; Adam 4,000 epochs (lr 0.015) followed by   L-BFGS 500 epochs; interior collocation points grow from 4,000 to   ~13,000 via R3. Reference run: a few minutes on GPU.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"burgers_eq/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>The domain is a rectangle; the initial condition and the two lateral boundaries are separate <code>Line</code> geometries:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\narea = df.geometry.rectangle([-1, 1], [0, 1])\nline_ic = df.geometry.line_horizontal(y=0, range_x=[-1, 1])\nline_bc1 = df.geometry.line_vertical(x=-1, range_y=[0, 1])\nline_bc2 = df.geometry.line_vertical(x=1, range_y=[0, 1])\ndomain = df.domain(area.area_list, line_ic, line_bc1, line_bc2)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"burgers_eq/#2-define-the-physics","title":"2. Define the physics","text":"<p>Attach the Burgers PDE to the area and the boundary/initial conditions to the lines. DeepFlow uses hard boundary conditions, so <code>define_bc</code> both constrains the network and generates the training data:</p> <pre><code>from torch import sin, pi\n\ndomain.area_list[0].define_pde(df.pde.BurgersEquation1D(nu=0.01 / pi))\ndomain.bound_list[0].define_bc({'u': ['x', lambda x: -sin(pi * x)]})  # IC at y=0\ndomain.bound_list[1].define_bc({'u': 0})                              # x = -1\ndomain.bound_list[2].define_bc({'u': 0})                              # x = +1\n</code></pre>"},{"location":"burgers_eq/#3-sample-training-data","title":"3. Sample training data","text":"<p>Initial sampling with Latin Hypercube Sampling (LHS), 2,000 points on the initial-condition line and 4,000 in the interior. <code>display_physics=True</code> colors the points by their role:</p> <pre><code>domain.sampling_lhs([2000, 1000, 1000], [4000])\ndomain.show_coordinates(display_physics=True)\n</code></pre> <p></p>"},{"location":"burgers_eq/#4-train-the-model","title":"4. Train the model","text":"<p>The shock front moves as training progresses, so a static point set wastes samples. The R3 scheme (arXiv:2207.02338) re-samples every 500 Adam epochs, concentrating points where the residual is largest:</p> <pre><code>def do_in_adam(epoch, model):\n    if epoch % 500 == 0 and epoch &gt; 0:\n        domain.sampling_R3([2000, 1000, 1000], [4000])\n        print(domain)\n\nmodel0 = df.PINN(input_vars=['x', 'y'], output_vars=['u'], width=16, length=4)\n\nmodel1, model1_best = model0.train_adam(\n    calc_loss=df.calc_loss_simple(domain),\n    learning_rate=0.015,\n    epochs=4000,\n    do_between_epochs=do_in_adam)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=500)\n</code></pre> <p>Reference-run result</p> <p>Adam (4,000 epochs) + L-BFGS (500 epochs) converged to <code>total_loss \u2248 2e-5</code>. The remaining error is dominated by the boundary residual near the shock front.</p> <p>After training, <code>show_coordinates</code> reveals how R3 has densified the point set around the shock:</p> <pre><code>domain.show_coordinates(display_physics=False)\n</code></pre> <p></p>"},{"location":"burgers_eq/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<p>Evaluate the trained network on a uniform grid and plot the field, the PDE residual, and the loss history:</p> <pre><code>prediction = domain.area_list[0].evaluate(model2)\nprediction.sampling_area([500, 250])\n\nprediction.plot_color('u', cmap='jet', s=0.3).savefig('u_field.png')\n_ = prediction.plot_color('pde_residual', cmap='rainbow', s=0.3)\n_ = prediction.plot('pde_residual')\n_ = prediction.plot_loss_curve(log_scale=True)\n</code></pre> <p> </p> <p>The shock front is clearly visible as a thin band of high PDE residual. A 1D slice at <code>y = 0.75</code> shows the steep front directly:</p> <pre><code>line = df.geometry.line_horizontal(y=0.75, range_x=[-1, 1])\nprediction = line.evaluate(model2)\nprediction.sampling_line(500)\n_ = prediction.plot(y_axis='u')\n</code></pre> <p></p>"},{"location":"burgers_eq/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<p>If the <code>[cfd]</code> extra is installed (NGSolve backend), DeepFlow can solve the same problem with a finite-element solver and report a mean absolute error against the PINN solution:</p> <pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    fem_reference = domain.solve_fem(\n        mesh_size=0.5,\n        boundary_resolution=16,\n        max_iterations=3,\n    )\n    fem_area = fem_reference.area_list[0]\n    fem_area.sampling_area([100, 100])\n    fem_values = fem_area['u_ref']\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_values)))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error: {u_error:.6e}')\n</code></pre> <p>See NGSolve reference solutions for the details of the FEM backend.</p>"},{"location":"burgers_eq/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/burgers_eq/burgers_eq.ipynb</code></p> <pre><code>jupyter notebook examples/burgers_eq/burgers_eq.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"cavity_flow_steady/","title":"Solving the Lid-Driven Cavity Flow (Steady)","text":"<p>This example solves the classic lid-driven cavity benchmark \u2014 a square cavity whose top wall slides at constant velocity. It is a standard validation case for incompressible flow solvers (COMSOL's benchmark write-up).</p> <p>At a glance</p> <ul> <li>Physics: steady incompressible Navier\u2013Stokes equations,   nondimensionalized with <code>(U, L) = (0.0001, 1)</code> and <code>mu = 0.001</code>,   <code>rho = 1000</code> \u2014 Reynolds number <code>Re = \u03c1UL/\u03bc = 100</code>. Lid: <code>u = 1</code>,   <code>v = 0</code>; other walls: no-slip; pressure pinned to <code>p = 0</code> at the   corner point.</li> <li>What to expect: a large primary vortex driven by the lid, with   secondary corner vortices at low Reynolds number. Verify: the   <code>u</code>-velocity profile is antisymmetric about the cavity mid-line.</li> <li>Setup cost: FNN 50 \u00d7 5 (outputs <code>u, v, p</code>); Adam 2,000 epochs   (lr 0.004) followed by L-BFGS 350 epochs (threshold 1e-4). Reference   run: a few minutes on GPU, final <code>total_loss \u2248 3e-4</code>.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"cavity_flow_steady/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>A square plus a corner point. The point is passed to the domain as a second geometry and receives the pressure reference condition:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\nrectangle = df.geometry.rectangle([0, 1], [0, 1])\npoint = df.geometry.point(0, 0)\n\ndomain = df.domain(rectangle, point)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"cavity_flow_steady/#2-define-the-physics","title":"2. Define the physics","text":"<pre><code>domain.bound_list[0].define_bc({'u': 0, 'v': 0})  # Bottom wall\ndomain.bound_list[1].define_bc({'u': 0, 'v': 0})  # Left wall\ndomain.bound_list[2].define_bc({'u': 0, 'v': 0})  # Right wall\ndomain.bound_list[3].define_bc({'u': 1, 'v': 0})  # Lid\ndomain.bound_list[4].define_bc({'p': 0})          # Corner point: pressure reference\ndomain.area_list[0].define_pde(df.NavierStokes(U=0.0001, L=1, mu=0.001, rho=1000))\ndomain.show_setup()\n</code></pre>"},{"location":"cavity_flow_steady/#3-sample-training-data","title":"3. Sample training data","text":"<p>1,000 LHS points per boundary (10 on the corner point) and 2,000 interior:</p> <pre><code>domain.sampling_lhs(bound_sampling_res=[1000, 1000, 1000, 1000, 10], area_sampling_res=[2000])\ndomain.show_coordinates(display_physics=False)\n</code></pre> <p></p>"},{"location":"cavity_flow_steady/#4-train-the-model","title":"4. Train the model","text":"<p>R3 resampling during Adam keeps the collocation points where the residual is largest:</p> <pre><code>def do_in_adam(epoch, model):\n    if epoch % 100 == 0 and epoch &gt; 0:\n        domain.sampling_R3(bound_sampling_res=[1000, 1000, 1000, 1000], area_sampling_res=[2000])\n        print(domain)\n\nmodel0 = df.PINN(width=50, length=5, input_vars=['x', 'y'], output_vars=['u', 'v', 'p'])\n\nmodel1, model1_best = model0.train_adam(\n    learning_rate=0.004,\n    epochs=2000,\n    calc_loss=df.calc_loss_simple(domain),\n    threshold_loss=0.005,\n    do_between_epochs=do_in_adam)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=350,\n    threshold_loss=0.0001)\n</code></pre> <p>Reference-run result</p> <p>Adam (2,000 epochs) + L-BFGS (350 epochs) reached <code>total_loss \u2248 3e-4</code>, below the 1e-4 threshold on the way to 3e-4. The BC residual dominates \u2014 the lid corners are the hardest part of this problem.</p> <p>After training, the resampled coordinates show where the solver spent its points:</p> <pre><code>domain.show_coordinates()\n</code></pre> <p></p> <p>Save (and reload) the trained model:</p> <pre><code>model2.save_as_pickle(\"model.pkl\")\nmodel2 = df.load_from_pickle(\"model.pkl\")\n</code></pre>"},{"location":"cavity_flow_steady/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<p>Evaluate on a uniform grid, derive the velocity magnitude, and plot the fields:</p> <pre><code>df.Visualizer.refwidth_default = 4\n\narea_eval = domain.area_list[0].evaluate(model2)\narea_eval.sampling_area([200, 200])\nprint(area_eval)\n\narea_eval['v_mag'] = (area_eval['u']**2 + area_eval['v']**2)**0.5\n\n_ = area_eval.plot_color('v_mag', s=1.5, cmap='jet').savefig(\"colorplot_v_mag.png\")\n_ = area_eval.plot_color('u', s=1.5, cmap='rainbow')\n_ = area_eval.plot_color('v', s=1.5, cmap='rainbow')\n_ = area_eval.plot_color('p', s=1.5, cmap='rainbow')\n_ = area_eval.plot_streamline('u', 'v', cmap='jet')\n</code></pre> <p> </p> <p>The streamlines show the primary clockwise vortex; the pressure field is pinned by the corner point condition. Plot the training loss:</p> <pre><code>_ = area_eval.plot_loss_curve(log_scale=True)\n</code></pre> <p></p>"},{"location":"cavity_flow_steady/#export-data","title":"Export data","text":"<pre><code>import numpy as np\n\nx_data = area_eval.data_dict['x']\ny_data = area_eval.data_dict['y']\nu_data = area_eval.data_dict['u']\n\narray = np.column_stack((x_data, y_data, u_data))\nnp.savetxt('outlet_velocity.txt', array)\n</code></pre>"},{"location":"cavity_flow_steady/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<p>With the <code>[cfd]</code> extra installed, compare against the NGSolve backend:</p> <pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    fem_reference = domain.solve_fem(\n        mesh_size=0.2,\n        boundary_resolution=32,\n        max_iterations=20,\n    )\n    fem_area = fem_reference.area_list[0]\n    fem_area.sampling_area([100, 100])\n    fem_values = fem_area['u_ref']\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_values)))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error: {u_error:.6e}')\n</code></pre>"},{"location":"cavity_flow_steady/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/cavity_flow_steady/cavity_flow_steady.ipynb</code></p> <pre><code>jupyter notebook examples/cavity_flow_steady/cavity_flow_steady.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to DeepFlow are recorded here, grouped by release. The format follows Keep a Changelog; releases are tagged on the <code>dev</code> branch.</p>"},{"location":"changelog/#013-in-development","title":"[0.1.3] - in development","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>ReferenceGroupEvaluator</code> \u2014 <code>solve_fem</code> now returns a directly queryable   reference solution (previously a plain solver result); per-geometry   evaluators re-query the FEM fields.</li> <li><code>GroupEvaluator</code> access API and <code>domain.evaluate()</code> \u2014 evaluators gained   <code>[]</code>-style field access (<code>area_eval['u']</code>, <code>fem_area['u_ref']</code>) alongside   <code>data_dict</code>; lazy evaluator field expressions.</li> <li><code>RFFPINN</code> \u2014 random Fourier feature network variant, with tests.</li> <li><code>StreamFunctionNavierStokes</code> PDE (stream-function formulation), with tests.</li> <li><code>max_grad_norm</code> gradient clipping in training methods.</li> <li>Configurable <code>weight_init</code> and Xavier initialization for FNN/PINN.</li> <li>Robust boolean geometry operations (<code>Area.__add__</code>, containment, robust   union) with tests.</li> <li>Unit tests for PINN training with Adam and L-BFGS optimizers.</li> <li>End-to-end reproducibility test \u2014 seeding now covers all RNGs.</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>Evaluation API refined (<code>Refine GroupEvaluator access API</code>): cleaner   field access and consistent contracts across steady and transient cases.</li> <li>FEM backend expanded: transient solves with time stepping, reference   solutions handling float32 inputs, default mesh size set.</li> <li>Training loop refactored: batched forward passes per physics type,   reduced peak memory in L-BFGS, consistent training status output.</li> <li>R3 resampling matches the original paper's algorithm; repeated random/LHS   sampling advances a shared RNG.</li> <li>Example notebooks aligned to the current API and standardized on R3   adaptive resampling; example docs pages cleaned and hand-authored (see   REGENERATE.md).</li> <li>Documentation: complete API reference and contract fixes; quickstart   training examples fixed; docs site restructured (Examples gallery,   Install, FAQ, Contributing, Changelog, Cite pages added).</li> </ul>"},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li><code>Area.__add__</code> crash and <code>train_adam</code>/<code>train_lbfgs</code> return-consistency.</li> <li>Hard-constraint loss and forward-mutation bugs in geometry evaluation.</li> <li><code>Area</code> union and subtraction edge cases.</li> <li>Non-finite loss now stops training cleanly.</li> <li>Windows case-sensitivity collisions removed.</li> <li>Missing <code>notebook</code> title/description swaps in example docs.</li> </ul>"},{"location":"changelog/#removed","title":"Removed","text":"<ul> <li>Obsolete benchmark scripts and experiment artifacts consolidated under   <code>EXPERIMENTS/</code> and <code>benchmarks/</code>.</li> <li>Duplicate doc assets (<code>docs/img/</code> tree) \u2014 one canonical <code>docs/static/</code>   tree plus the README's root <code>static/</code>.</li> </ul>"},{"location":"changelog/#012-2026-02-03","title":"[0.1.2] - 2026-02-03","text":"<p>Initial tagged release.</p>"},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>First complete set of example demos (Burgers, cavity, cylinder, heat   equation, transient channel flow) and their documentation pages.</li> <li>Animation support for transient solutions (<code>plot_animate</code>).</li> <li>Heat equation example and <code>HeatEquation</code> PDE.</li> <li>Better-plot visualization layer (ultraplot-based) and promo assets.</li> </ul>"},{"location":"changelog/#fixed_1","title":"Fixed","text":"<ul> <li>Various demo bugs, R3 sampling edge cases, hard-BC handling.</li> <li>Repository metadata, README badges, and requirements.</li> </ul>"},{"location":"changelog/#docs","title":"Docs","text":"<ul> <li>First MkDocs prototype and GitHub Pages deployment workflow.</li> </ul>"},{"location":"changelog/#older-history","title":"Older history","text":"<p>Untagged commits before <code>v0.1.2</code> \u2014 see <code>git log v0.1.2</code>.</p>"},{"location":"channel_flow_transient/","title":"Solving the Transient Channel Flow","text":"<p>This example solves the start-up of incompressible flow in a channel: the fluid is initially at rest and accelerates under a parabolic inflow until a steady Poiseuille-like profile is established. It is the introductory example for time-dependent problems \u2014 the network takes <code>t</code> as an input and the domain is given a time range.</p> <p>At a glance</p> <ul> <li>Physics: unsteady incompressible Navier\u2013Stokes equations on   <code>[0, 5] \u00d7 [0, 1]</code>, <code>t \u2208 [0, 10]</code>, with <code>mu = 0.001</code>, <code>rho = 1000</code>,   <code>U = 0.001</code> (Reynolds number <code>Re = \u03c1UL/\u03bc = 1</code>). Parabolic inflow   (max <code>u = 1</code> at mid-height), no-slip walls, <code>p = 0</code> at the outlet,   initial condition <code>u = v = 0</code>.</li> <li>What to expect: the parabolic inflow profile propagates through the   channel; the flow approaches a steady Poiseuille profile. The animation   shows the <code>u</code> field from rest to quasi-steady state.</li> <li>Setup cost: FNN 50 \u00d7 5 (inputs <code>x, y, t</code>); Adam 1,000 epochs   (lr 0.001) followed by L-BFGS 350 epochs (threshold 1e-4). Reference   run: a few minutes on GPU, final <code>total_loss \u2248 8e-4</code>.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"channel_flow_transient/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>Two overlapping rectangles: the first carries the PDE and boundary conditions, the second carries the initial condition:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\nrectangle_pde = df.geometry.rectangle([0, 5], [0, 1])\nrectange_ic = df.geometry.rectangle([0, 5], [0, 1])\n\ndomain = df.domain(rectangle_pde, rectange_ic.area_list)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"channel_flow_transient/#2-define-the-physics","title":"2. Define the physics","text":"<pre><code>domain.bound_list[0].define_bc({'u': df.parabolic('y', 1, 1, 0.5), 'v': 0})  # Inlet\ndomain.bound_list[1].define_bc({'u': 0, 'v': 0})  # Bottom wall\ndomain.bound_list[2].define_bc({'p': 0})          # Outlet\ndomain.bound_list[3].define_bc({'u': 0, 'v': 0})  # Top wall\ndomain.area_list[0].define_pde(df.NavierStokes(U=0.001, L=1, mu=0.001, rho=1000))\ndomain.area_list[1].define_ic({'u': 0, 'v': 0})\n\nfor geom in domain:\n    geom.define_time([0, 10], 'random')\n\ndomain.show_setup()\n</code></pre> <p>Note</p> <p><code>define_time</code> without an explicit <code>expo_scaling</code> argument emits the warning <code>expo_scaling has not yet defined. False is set as default.</code> \u2014 that is expected; pass <code>expo_scaling=False</code> to silence it (see the FAQ).</p>"},{"location":"channel_flow_transient/#3-sample-training-data","title":"3. Sample training data","text":"<pre><code>domain.sampling_lhs(bound_sampling_res=[1000, 1000, 1000, 1000], area_sampling_res=[2000, 2000])\ndomain.show_coordinates(display_physics=False)\n</code></pre>"},{"location":"channel_flow_transient/#4-train-the-model","title":"4. Train the model","text":"<p>R3 resampling runs every 100 Adam epochs. The notebook also verifies that resampling never empties a region of the time interval \u2014 important for transient problems:</p> <pre><code>import torch\n\ndef check_time_coverage():\n    for geometry in domain.bound_list + domain.area_list:\n        if geometry.physics_type == 'IC' or not isinstance(geometry.range_t, (tuple, list)):\n            continue\n        times = geometry.t\n        start, end = map(float, geometry.range_t)\n        edges = torch.linspace(start, end, 9, dtype=times.dtype, device=times.device)\n        covered_bins = torch.unique(torch.bucketize(times.detach(), edges[1:-1])).numel()\n        if covered_bins &lt; 8:\n            raise RuntimeError(f'Insufficient time coverage after R3: {covered_bins}/8 bins')\n    print('R3 time coverage: all non-initial geometries span the full time interval')\n\ndef do_in_adam(epoch, model):\n    if epoch % 100 == 0 and epoch &gt; 0:\n        domain.sampling_R3(bound_sampling_res=[1000, 1000, 1000, 1000], area_sampling_res=[2000, 2000])\n        check_time_coverage()\n        print(domain)\n\ncheck_time_coverage()\n\nmodel0 = df.PINN(width=50, length=5, input_vars=['x', 'y', 't'], output_vars=['u', 'v', 'p'])\n\nmodel1, model1_best = model0.train_adam(\n    learning_rate=0.001,\n    epochs=1000,\n    calc_loss=df.calc_loss_simple(domain),\n    threshold_loss=0.005,\n    do_between_epochs=do_in_adam)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=350,\n    threshold_loss=0.0001)\n</code></pre> <p>Reference-run result</p> <p>Adam (1,000 epochs) + L-BFGS (350 epochs) reached <code>total_loss \u2248 8e-4</code>, short of the 1e-4 threshold. The IC residual (start-up front) and the BC residual near the inlet are the dominant terms.</p> <pre><code>domain.show_coordinates()\n</code></pre> <p></p> <p>Save (and reload) the trained model:</p> <pre><code>model2.save_as_pickle(\"model.pkl\")\nmodel2 = df.load_from_pickle(\"model.pkl\")\n</code></pre>"},{"location":"channel_flow_transient/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<p>Evaluate at <code>t = 10</code> and plot the fields:</p> <pre><code>df.Visualizer.refwidth_default = 6\n\narea_eval = domain.area_list[0].evaluate(model2)\narea_eval.sampling_area([400, 80])\narea_eval.define_time(10)\nprint(area_eval)\n\narea_eval['v_mag'] = (area_eval['u']**2 + area_eval['v']**2)**0.5\n\n_ = area_eval.plot_color('v_mag', s=2, cmap='jet').savefig(\"colorplot_v_mag.png\")\n_ = area_eval.plot_color('u', s=2, cmap='rainbow')\n_ = area_eval.plot_color('v', s=2, cmap='rainbow')\n_ = area_eval.plot_color('p', s=2, cmap='rainbow')\n_ = area_eval.plot_streamline('u', 'v', cmap='jet')\n_ = area_eval.plot_color('pde_residual', s=2, cmap='viridis')\n</code></pre> <p> </p> <p>Animate the <code>u</code> field from rest to quasi-steady state:</p> <pre><code>anim = area_eval.plot_animate(\n    'u',\n    range_t=[0.05, 8.1],\n    dt=0.1,\n    frame_interval=100,\n    cmap='rainbow',\n    s=1,\n    color_range=[0, 1],\n)\nanim.save(\"animation.gif\")\n</code></pre> <p></p> <p>Training loss:</p> <pre><code>_ = area_eval.plot_loss_curve(log_scale=True)\n</code></pre> <p></p>"},{"location":"channel_flow_transient/#export-data","title":"Export data","text":"<pre><code>import numpy as np\n\nx_data = area_eval.data_dict['x']\ny_data = area_eval.data_dict['y']\nu_data = area_eval.data_dict['u']\n\narray = np.column_stack((x_data, y_data, u_data))\nnp.savetxt('outlet_velocity.txt', array)\n</code></pre>"},{"location":"channel_flow_transient/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<p>The NGSolve backend solves the transient problem with time stepping; the mean absolute <code>u</code> error is reported at the final time:</p> <pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    time_interval = [0, 10]\n    domain.area_list[0].define_time(time_interval, sampling_scheme='random')\n    fem_reference = domain.solve_fem(\n        mesh_size=0.4,\n        boundary_resolution=32,\n        time_step=1.0,\n        max_iterations=10,\n    )\n    fem_area = fem_reference.area_list[0]\n    query_time = float(time_interval[1])\n    fem_area.sampling_area([100, 40])\n    fem_area.define_time(query_time)\n    fem_values = fem_area['u_ref']\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_values)))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error at t={query_time:g}: {u_error:.6e}')\n</code></pre>"},{"location":"channel_flow_transient/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/channel_flow_transient/channel_flow_transient.ipynb</code></p> <pre><code>jupyter notebook examples/channel_flow_transient/channel_flow_transient.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"cite/","title":"Citing DeepFlow","text":"<p>If DeepFlow contributes to a project that leads to a scientific publication, please cite it. The project does not yet have a paper or DOI; use the repository citation below.</p>"},{"location":"cite/#repository-citation","title":"Repository citation","text":"<p>Thammatorn Jamraschai. DeepFlow: Physics-Informed Neural Networks for Fluid Dynamics (v0.1.3). GitHub repository, 2026. https://github.com/YoYo-XYZ/pinn-deepflow</p>"},{"location":"cite/#bibtex","title":"BibTeX","text":"<pre><code>@misc{deepflow,\n  author       = {Jamraschai, Thammatorn},\n  title        = {{DeepFlow}: {Physics-Informed} {Neural} {Networks} for {Fluid} {Dynamics}},\n  year         = {2026},\n  note         = {Version 0.1.3},\n  howpublished = {\\url{https://github.com/YoYo-XYZ/pinn-deepflow}},\n}\n</code></pre>"},{"location":"cite/#background-reading","title":"Background reading","text":"<p>The methods DeepFlow builds on are described in the following papers:</p> <ul> <li>Physics-informed neural networks (Raissi, Perdikaris, Karniadakis 2019):   arXiv:1711.10561</li> <li>RAR-G adaptive sampling:   arXiv:2207.10289</li> <li>R3 residual-based adaptive refinement:   arXiv:2207.02338</li> </ul>"},{"location":"cite/#license","title":"License","text":"<p>DeepFlow is released under the MIT License.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>Thanks for considering a contribution to DeepFlow. This page explains how the repository is organized and how to develop, test, and submit changes.</p>"},{"location":"contributing/#repository-layout","title":"Repository layout","text":"<pre><code>src/deepflow/          Package source (src layout)\n  geometry.py          Geometries and boolean operations\n  domain.py            Domains, boundaries, sampling (incl. R3)\n  pde.py               PDE implementations and the PDE base class\n  nn.py                FNN/PINN/RFFPINN models and training (Adam/L-BFGS)\n  physicsinformed.py   Physics attachment and evaluation glue\n  evaluation.py        Evaluators (GroupEvaluator, ReferenceGroupEvaluator)\n  visualization.py     Plotting (ultraplot-based)\n  reference/           NGSolve FEM backend (optional)\nexamples/              Runnable notebooks; one folder per example\ntests/                 Unit and regression tests (pytest)\nbenchmarks/            Comparative benchmark scripts\nEXPERIMENTS/           Comparative experiments and reports\ndocs/                  MkDocs site source\nstatic/                Images used by the GitHub README\n</code></pre> <p>The intended and correct usage of the API is demonstrated in <code>examples/</code> \u2014 if you change the API, update the corresponding example and its docs page (see REGENERATE.md).</p>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<p>The repository uses <code>uv</code> (a <code>uv.lock</code> is committed). With uv:</p> <pre><code>uv sync\n</code></pre> <p>or with plain pip:</p> <pre><code>python -m venv .venv\n.\\.venv\\Scripts\\activate        # Windows; see venv docs for other shells\npip install -e .\n</code></pre> <p>To run the FEM reference comparisons locally, additionally install the optional extra:</p> <pre><code>pip install -e \".[cfd]\"\n</code></pre>"},{"location":"contributing/#running-tests","title":"Running tests","text":"<pre><code>pytest tests/\n</code></pre> <p>The test suite covers geometry, sampling, PDE residuals, training, the FEM backend, and the documentation example workflows. Run it before pushing.</p>"},{"location":"contributing/#building-the-docs-locally","title":"Building the docs locally","text":"<pre><code>pip install -r requirements-docs.txt\nmkdocs serve\n</code></pre> <p><code>mkdocs build --strict</code> is enforced by CI (<code>docs-check.yml</code>) on every pull request that touches <code>docs/</code> \u2014 broken links and missing images fail the build, so build locally before opening the PR.</p>"},{"location":"contributing/#pull-request-workflow","title":"Pull request workflow","text":"<ol> <li>Open an issue first to discuss major changes \u2014 the maintainers prefer to    agree on direction before large diffs.</li> <li>Create a feature branch from <code>dev</code>; the docs deploy workflow publishes    from <code>dev</code>.</li> <li>Keep changes surgical: touch only what your change requires, and    follow the style rules in <code>AGENTS.md</code> (simplicity first; no speculative    features; match existing style).</li> <li>Add or update tests for new behavior, and update examples/docs that show    the affected API.</li> <li>Run <code>pytest tests/</code> and <code>mkdocs build --strict</code>, then open the pull    request against <code>dev</code>.</li> </ol>"},{"location":"contributing/#code-style","title":"Code style","text":"<ul> <li>Minimum code that solves the problem \u2014 no speculative features or   single-use abstractions.</li> <li>Match the existing style of the file you are editing.</li> <li>New public API must come with docstrings (Google style) \u2014 the API   reference is generated from them via mkdocstrings.</li> <li>Do not commit secrets, generated artifacts, or stray notebooks outputs.</li> </ul>"},{"location":"contributing/#release-process","title":"Release process","text":"<p>Releases are tagged on <code>dev</code> (e.g. <code>v0.1.3</code>) and the changelog in <code>docs/changelog.md</code> is updated by hand per release.</p>"},{"location":"cylinder_flow_steady/","title":"Solving Steady Flow around a Cylinder","text":"<p>This example solves steady incompressible flow around a circular cylinder in a channel \u2014 a classic benchmark for flow solvers, here following the setup of arXiv:2002.10558. It demonstrates boolean geometry (rectangle minus circle) and a non-trivial inflow profile.</p> <p>At a glance</p> <ul> <li>Physics: steady incompressible Navier\u2013Stokes equations with   <code>mu = 0.02</code>, <code>rho = 1</code>, <code>U = 1</code> (<code>Re \u2248 50</code> on the unit length scale).   Parabolic inflow with maximum <code>u = 1</code> at mid-height, no-slip walls and   cylinder surface, <code>p = 0</code> at the outlet.</li> <li>What to expect: a steady, symmetric wake behind the cylinder with a   small recirculation zone \u2014 at this Reynolds number there is no vortex   shedding (that requires <code>Re \u2248 40\u201347</code> based on the cylinder diameter).   Verify the wake symmetry about the channel mid-line.</li> <li>Setup cost: FNN 32 \u00d7 5 (outputs <code>u, v, p</code>); Adam 2,000 epochs   (lr 0.004) followed by L-BFGS 450 epochs (threshold 1e-4); 4,000   interior points growing to ~7,000 via R3. Reference run: a few minutes   on GPU.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"cylinder_flow_steady/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>The cylinder is subtracted from the channel with the <code>-</code> operator; its boundaries are passed to the domain so they get sampled as obstacles:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\ncircle = df.geometry.circle(0.2, 0.2, 0.05)\nrectangle = df.geometry.rectangle([0, 1.1], [0, 0.41])\narea = rectangle - circle\n\ndomain = df.domain(area, circle.bound_list)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"cylinder_flow_steady/#2-define-the-physics","title":"2. Define the physics","text":"<pre><code>domain.bound_list[0].define_bc({'u': ['y', lambda x: 4 * 1 * (0.41 - x) * x / 0.41**2], 'v': 0})  # Inlet\ndomain.bound_list[1].define_bc({'u': 0, 'v': 0})  # Bottom wall\ndomain.bound_list[2].define_bc({'p': 0})          # Outlet\ndomain.bound_list[3].define_bc({'u': 0, 'v': 0})  # Top wall\ndomain.bound_list[4].define_bc({'u': 0, 'v': 0})  # Cylinder\ndomain.bound_list[5].define_bc({'u': 0, 'v': 0})  # Cylinder\ndomain.area_list[0].define_pde(df.NavierStokes(U=1, L=1, mu=0.02, rho=1))\ndomain.show_setup()\n</code></pre>"},{"location":"cylinder_flow_steady/#3-sample-training-data","title":"3. Sample training data","text":"<pre><code>domain.sampling_lhs(bound_sampling_res=[1000, 1000, 1000, 1000, 1000, 1000], area_sampling_res=[4000])\ndomain.show_coordinates(display_physics=False)\n</code></pre>"},{"location":"cylinder_flow_steady/#4-train-the-model","title":"4. Train the model","text":"<pre><code>def do_in_adam(epoch, model):\n    if epoch % 100 == 0 and epoch &gt; 0:\n        domain.sampling_R3(bound_sampling_res=[1000, 1000, 1000, 1000, 1000, 1000], area_sampling_res=[4000])\n        print(domain)\n\nmodel0 = df.PINN(width=32, length=5, input_vars=['x', 'y'], output_vars=['u', 'v', 'p'])\n\nmodel1, model1_best = model0.train_adam(\n    learning_rate=0.004,\n    epochs=2000,\n    calc_loss=df.calc_loss_simple(domain),\n    threshold_loss=0.01,\n    do_between_epochs=do_in_adam)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=450,\n    threshold_loss=0.0001)\n</code></pre> <p>Reference-run result</p> <p>Adam (2,000 epochs) dropped <code>total_loss</code> to \u2248 0.2; L-BFGS (450 epochs) pushed it below 1e-3 before the run was interrupted. The momentum residuals near the cylinder surface dominate the remaining error.</p> <pre><code>domain.show_coordinates()\n</code></pre> <p></p> <p>Save (and reload) the trained model:</p> <pre><code>model2.save_as_pickle(\"model.pkl\")\nmodel2 = df.load_from_pickle(\"model.pkl\")\n</code></pre>"},{"location":"cylinder_flow_steady/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<pre><code>area_eval = domain.area_list[0].evaluate(model2)\narea_eval.sampling_area([300, 150])\nprint(area_eval)\n\n_ = area_eval.plot_color('u', s=2, cmap='rainbow').savefig(\"colorplot_u.png\")\n_ = area_eval.plot_color('v', s=2, cmap='rainbow')\n_ = area_eval.plot_color('p', s=2, cmap='rainbow')\n_ = area_eval.plot_streamline('u', 'v', cmap='jet')\n_ = area_eval.plot('pde_residual')\n</code></pre> <p>The streamlines show the steady recirculation zone behind the cylinder. The residual plot concentrates error near the cylinder and the inlet corners.</p> <p>Check the outflow profile against the inflow: the outlet (<code>bound_list[2]</code>) should carry a parabolic <code>u</code> profile:</p> <pre><code>bound_visual = domain.bound_list[2].evaluate(model2)\nbound_visual.sampling_line(200)\n\n_ = bound_visual.plot_color('u', cmap='rainbow')\n_ = bound_visual.plot(x_axis='y', y_axis='u')\n</code></pre> <p> </p> <p>Training loss:</p> <pre><code>_ = bound_visual.plot_loss_curve(log_scale=True)\n</code></pre> <p></p>"},{"location":"cylinder_flow_steady/#export-data","title":"Export data","text":"<pre><code>import numpy as np\n\nx_data = bound_visual.data_dict['x']\ny_data = bound_visual.data_dict['y']\nu_data = bound_visual.data_dict['u']\n\narray = np.column_stack((x_data, y_data, u_data))\nnp.savetxt('outlet_velocity.txt', array)\n</code></pre>"},{"location":"cylinder_flow_steady/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<p>The NGSolve backend solves the same steady problem on a triangular mesh; <code>fem_reference.metadata</code> reports the solve configuration, and <code>u_ref</code> is the FEM field queried at the same coordinates:</p> <pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    fem_reference = domain.solve_fem(\n        mesh_size=0.05,\n        boundary_resolution=64,\n        max_iterations=200,\n    )\n    fem_area = fem_reference.area_list[0]\n    fem_area.sampling_area([300, 150])\n    print(fem_reference.metadata)\n    _ = fem_area.plot_color('u_ref', s=2, cmap='rainbow')\n\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_area.data_dict['u_ref'])))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error: {u_error:.6e}')\n</code></pre>"},{"location":"cylinder_flow_steady/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/cylinder_flow_steady/cylinder_flow_steady.ipynb</code></p> <pre><code>jupyter notebook examples/cylinder_flow_steady/cylinder_flow_steady.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"examples/","title":"Examples","text":"<p>Six worked examples ship with DeepFlow, each as a Jupyter notebook in <code>examples/</code> with a hand-authored documentation page. Every page states which DeepFlow version it was verified against, reports a reference-run result, and links back to its source notebook.</p>"},{"location":"examples/#steady-problems","title":"Steady problems","text":""},{"location":"examples/#burgers-equation","title":"Burgers' Equation","text":"<p>Challenging because of the sharp shock front that demands adaptive resampling (R3) to concentrate collocation points where the residual lives.</p> <p>Solve it \u2192 \u00b7 <code>examples/burgers_eq/burgers_eq.ipynb</code></p>"},{"location":"examples/#lid-driven-cavity-flow","title":"Lid-driven Cavity Flow","text":"<p>The classic benchmarking case: a square cavity with a sliding lid, used to validate incompressible solvers at <code>Re = 100</code>.</p> <p>Solve it \u2192 \u00b7 <code>examples/cavity_flow_steady/cavity_flow_steady.ipynb</code></p>"},{"location":"examples/#flow-around-a-cylinder","title":"Flow around a Cylinder","text":"<p>Steady flow past a circular cylinder in a channel \u2014 boolean geometry (rectangle minus circle) with a parabolic inflow and a steady symmetric wake.</p> <p>Solve it \u2192 \u00b7 <code>examples/cylinder_flow_steady/cylinder_flow_steady.ipynb</code></p>"},{"location":"examples/#backward-facing-step","title":"Backward-facing Step","text":"<p>The recirculation benchmark: flow separates at a step corner and reattaches downstream, forming a recirculation zone whose length you can check against the literature.</p> <p>Solve it \u2192 \u00b7 <code>examples/BFS_flow_steady/BFS_flow_steady.ipynb</code></p>"},{"location":"examples/#transient-problems","title":"Transient problems","text":""},{"location":"examples/#transient-channel-flow","title":"Transient Channel Flow","text":"<p>Start-up flow in a channel: the fluid accelerates from rest to a steady Poiseuille profile, visualized as an animation. The introduction to time-dependent problems (<code>t</code> as a network input).</p> <p>Solve it \u2192 \u00b7 <code>examples/channel_flow_transient/channel_flow_transient.ipynb</code></p>"},{"location":"examples/#2d-heat-equation","title":"2D Heat Equation","text":"<p>Transient diffusion on a square with a hot top wall \u2014 a compact, fast example that introduces time ranges, initial conditions, and animated solutions.</p> <p>Solve it \u2192 \u00b7 <code>examples/heat_eq/heat_eq.ipynb</code></p> <p>Every example ends with an optional FEM comparison that runs when the <code>deepflow[cfd]</code> extra (NGSolve backend) is installed \u2014 see NGSolve reference solutions.</p>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#when-should-i-use-fp64","title":"When should I use FP64?","text":"<p>Recent PINN research shows that double precision improves convergence and accuracy, at the cost of speed and memory. Switch the entire pipeline \u2014 model weights, sampled coordinates, and PDE residuals \u2014 before building anything:</p> <pre><code>import torch\nimport deepflow as df\n\ndf.dtype = torch.float64\n\n# ... define geometry, PDE, sample, build model, and train as usual\n</code></pre> <p>Switch back with <code>df.dtype = torch.float32</code>. Only <code>float32</code> and <code>float64</code> are supported. Reach for FP64 when a model converges to a plateau that is still above the target loss, or when residuals at sharp gradients look noisy.</p>"},{"location":"faq/#cpu-or-gpu","title":"CPU or GPU?","text":"<p>DeepFlow auto-selects <code>'cuda'</code> when available (<code>df.device</code> reports the choice). Force the CPU with:</p> <pre><code>df.device = 'cpu'\n</code></pre> <p>CPU works for all examples but expect order-of-magnitude slower training \u2014 the reference runs on the example pages used a GPU.</p>"},{"location":"faq/#which-sampling-method-should-i-use","title":"Which sampling method should I use?","text":"Method When <code>sampling_uniform</code> / <code>sampling_random</code> Quick first experiments on simple domains <code>sampling_lhs</code> Default choice for a good static initial point set <code>sampling_RAR</code> Add points where the residual is largest (top-k of random candidates) <code>sampling_R3</code> Recommended for training: re-sample collocation points proportionally to the residual, replacing the old set (see arXiv:2207.02338) <code>sampling_R3_</code> Like R3 but the residual itself is used as the sampling density (see arXiv:2301.04744) <code>sampling_accumulate</code> Keep the old points and add new ones <p>A static point set is a fixed resource; for problems with moving features (shocks, fronts, wakes) use R3 inside a training callback:</p> <pre><code>def do_in_adam(epoch, model):\n    if epoch % 500 == 0 and epoch &gt; 0:\n        domain.sampling_R3([2000, 1000, 1000], [4000])\n</code></pre>"},{"location":"faq/#my-loss-is-stuck-what-should-i-try-first","title":"My loss is stuck. What should I try first?","text":"<ol> <li>Check the training schedule \u2014 the working pattern is Adam first (lr    ~1e-3\u20131e-2) to get into the basin, then L-BFGS to polish. L-BFGS alone    from a bad start often stalls.</li> <li>Resample \u2014 static points can over- or under-cover sharp features;    switch on R3 with a moderate frequency.</li> <li>Lower the learning rate \u2014 if Adam oscillates (loss jumps up after    long flat stretches), try <code>learning_rate=1e-4</code>\u2013<code>1e-3</code>.</li> <li>Switch to FP64 \u2014 the fastest fix for residual plateaus.</li> <li>Give L-BFGS a threshold and budget \u2014 <code>threshold_loss</code> stops early    when converged; a too-small <code>epochs</code> leaves it mid-run.</li> <li>Inspect the loss terms \u2014 print the per-term losses (BC vs PDE vs IC).    A stuck BC term usually means a mis-specified boundary condition or an    impossible-to-satisfy constraint, not an optimization problem.</li> </ol>"},{"location":"faq/#what-is-the-difference-between-hard-and-soft-boundary-conditions","title":"What is the difference between hard and soft boundary conditions?","text":"<p>DeepFlow's <code>define_bc</code> implements hard boundary conditions: the network output is constructed to satisfy the BC exactly by construction, so the BC term can be dropped from the loss. This is why <code>calc_loss_simple</code> returns clean physics-only training data. The IC (for transient problems) is handled the same way via <code>define_ic</code>. Consequences: no BC-vs-PDE loss weighting to tune, but not every condition can be enforced exactly \u2014 derivative conditions and certain custom conditions fall back to soft treatment (a residual term).</p>"},{"location":"faq/#can-i-define-a-custom-pde","title":"Can I define a custom PDE?","text":"<p>Yes. Subclass <code>df.pde.PDE</code>, implement <code>compute_residuals(inputs_dict)</code>, and store residuals in <code>self.var</code> / return them as tensors:</p> <pre><code>import torch\nimport deepflow as df\n\nclass MyPDE(df.pde.PDE):\n    def compute_residuals(self, inputs_dict):\n        # inputs_dict has coordinates and predicted fields\n        ...\n        return (residual,)\n\ndomain.area_list[0].define_pde(MyPDE())\n</code></pre> <p>See <code>src/deepflow/pde.py</code> for the built-in implementations to model yours on. Custom PDEs with hard BCs need the boundary condition syntax to know which field constrains which coordinate.</p>"},{"location":"faq/#how-do-i-use-the-fem-reference-backend","title":"How do I use the FEM reference backend?","text":"<pre><code>pip install \"deepflow[cfd]\"\n</code></pre> <p>The NGSolve/Netgen backend solves DeepFlow's built-in PDEs on unstructured meshes and returns a <code>ReferenceGroupEvaluator</code> with per-geometry fields (<code>u_ref</code>, <code>v_ref</code>, ...). See NGSolve reference solutions and the FEM comparison section at the end of every example page.</p>"},{"location":"faq/#why-does-define_time-warn-about-expo_scaling","title":"Why does <code>define_time</code> warn about <code>expo_scaling</code>?","text":"<p><code>expo_scaling</code> is an experimental flag for exponential time scaling; it defaults to <code>False</code> and warns until you set it explicitly:</p> <pre><code>g.define_time(range_t=[0, 1], sampling_scheme='random', expo_scaling=False)\n</code></pre>"},{"location":"faq/#how-do-i-force-reproducibility","title":"How do I force reproducibility?","text":"<pre><code>df.manual_seed(69)\n</code></pre> <p>This seeds the global RNGs (including sampling). Training on a GPU may still introduce nondeterminism from non-deterministic kernels.</p>"},{"location":"heat_eq/","title":"Solving the 2D Unsteady Heat Equation","text":"<p>This example solves the transient 2D Fourier heat equation on a square with a hot top boundary. It introduces DeepFlow's time handling: geometries are given a time range, and the network takes <code>t</code> as an input variable.</p> <p>At a glance</p> <ul> <li>Physics: <code>u_t = \u03b1 (u_xx + u_yy)</code> with <code>\u03b1 = 0.1</code> on <code>[0, 1]\u00b2</code>,   <code>t \u2208 [0, 1]</code>. Boundary conditions: <code>u = 0</code> on the left, bottom, and   right walls, <code>u = 1</code> on the top wall. Initial condition: <code>u = 0</code>.</li> <li>What to expect: heat diffuses downward from the hot top wall; the   solution is symmetric about <code>x = 0.5</code>. The animation shows the   temperature field evolving from the initial state to steady diffusion.</li> <li>Setup cost: FNN 32 \u00d7 4 (inputs <code>x, y, t</code>); Adam 2,000 epochs   (lr 0.004) followed by L-BFGS 450 epochs (threshold 5e-3). Reference   run: a few minutes on GPU.</li> <li>Verified against: DeepFlow v0.1.3 (commit <code>828f392</code>).</li> </ul>"},{"location":"heat_eq/#1-define-the-geometry","title":"1. Define the geometry","text":"<p>Two overlapping rectangles make up the domain: the first carries the PDE and boundary conditions, the second carries the initial condition:</p> <pre><code>import deepflow as df\n\ndf.manual_seed(69)  # for reproducibility\n\nrectangle = df.geometry.rectangle([0, 1], [0, 1])\nrectangle1 = df.geometry.rectangle([0, 1], [0, 1])\ndomain = df.domain(rectangle, rectangle1.area_list)\ndomain.show_setup()\n</code></pre> <p></p>"},{"location":"heat_eq/#2-define-the-physics","title":"2. Define the physics","text":"<pre><code>domain.bound_list[0].define_bc({'u': 0})  # Left wall\ndomain.bound_list[1].define_bc({'u': 0})  # Bottom wall\ndomain.bound_list[2].define_bc({'u': 0})  # Right wall\ndomain.bound_list[3].define_bc({'u': 1})  # Top wall: hot\n\ndomain.area_list[0].define_pde(df.pde.HeatEquation(0.1))\ndomain.area_list[1].define_ic({'u': 0})\n\nfor g in domain:\n    g.define_time(range_t=[0, 1], sampling_scheme='random')\n\ndomain.show_setup()\n</code></pre> <p>Note</p> <p><code>define_time</code> without an explicit <code>expo_scaling</code> argument emits the warning <code>expo_scaling has not yet defined. False is set as default.</code> \u2014 that is expected behavior; pass <code>expo_scaling=False</code> to silence it (see the FAQ).</p>"},{"location":"heat_eq/#3-sample-training-data","title":"3. Sample training data","text":"<pre><code>domain.sampling_lhs([1000, 1000, 2000, 1000], [2000, 2000])\ndomain.show_coordinates(display_physics=True)\n</code></pre>"},{"location":"heat_eq/#4-train-the-model","title":"4. Train the model","text":"<p>R3 resampling is applied every 1,000 Adam epochs. The notebook also checks that resampling does not empty any region of the time interval:</p> <pre><code>import torch\n\ndef check_time_coverage():\n    for geometry in domain.bound_list + domain.area_list:\n        if geometry.physics_type == 'IC' or not isinstance(geometry.range_t, (tuple, list)):\n            continue\n        times = geometry.t\n        start, end = map(float, geometry.range_t)\n        edges = torch.linspace(start, end, 9, dtype=times.dtype, device=times.device)\n        covered_bins = torch.unique(torch.bucketize(times.detach(), edges[1:-1])).numel()\n        if covered_bins &lt; 8:\n            raise RuntimeError(f'Insufficient time coverage after R3: {covered_bins}/8 bins')\n    print('R3 time coverage: all non-initial geometries span the full time interval')\n\ndef do_in_adam(epoch, model):\n    if epoch % 1000 == 0 and epoch &gt; 0:\n        domain.sampling_R3([1000, 1000, 2000, 1000], [2000, 2000])\n        check_time_coverage()\n        print(domain)\n\ncheck_time_coverage()\n\nmodel0 = df.PINN(width=32, length=4, input_vars=['x', 'y', 't'], output_vars=['u'])\nmodel1, model1_best = model0.train_adam(\n    calc_loss=df.calc_loss_simple(domain),\n    learning_rate=0.004,\n    do_between_epochs=do_in_adam,\n    epochs=2000)\n\nmodel2, model2_best = model1_best.train_lbfgs(\n    calc_loss=df.calc_loss_simple(domain),\n    epochs=450,\n    threshold_loss=5e-3)\n</code></pre> <p>Reference-run result</p> <p>Adam (2,000 epochs) + L-BFGS (450 epochs) reached <code>total_loss \u2248 1e-2</code> (threshold 5e-3) before the run was interrupted; the IC and BC residuals are the dominant terms. Longer L-BFGS runs converge further \u2014 the threshold and epoch count are the knobs to turn first.</p>"},{"location":"heat_eq/#5-visualize-the-solution","title":"5. Visualize the solution","text":"<p>Evaluate at a fixed time <code>t = 0.5</code> and plot the temperature field and the loss history:</p> <pre><code>prediction = domain.area_list[0].evaluate(model2)\nprediction.sampling_area([200, 200])\nprediction.define_time(0.5)\n\n_ = prediction.plot('u', color='plasma')\n_.savefig('heat_eq_u.png', dpi=200)\n_ = prediction.plot_loss_curve(log_scale=True, keys=['total_loss'])\n</code></pre> <p> </p> <p>Animate the solution over time:</p> <pre><code>prediction.sampling_area([160, 160])\nprediction.plot_animate(\n    'u',\n    range_t=[0.02, 1.02],\n    dt=0.02,\n    frame_interval=100,\n    cmap='plasma',\n    plot_type='scatter',\n    s=1.7,\n).save('heat_equation.mp4', dpi=200)\n</code></pre>"},{"location":"heat_eq/#6-optional-fem-reference-comparison","title":"6. Optional: FEM reference comparison","text":"<p>With the <code>[cfd]</code> extra installed, the transient problem can also be solved by the NGSolve backend:</p> <pre><code>try:\n    import ngsolve  # noqa: F401\nexcept (ImportError, OSError) as exc:\n    print(f'Skipping optional FEM comparison: {exc}')\nelse:\n    import numpy as np\n\n    time_interval = [0, 1]\n    domain.area_list[0].define_time(time_interval, sampling_scheme='random')\n    fem_reference = domain.solve_fem(\n        mesh_size=0.1,\n        boundary_resolution=32,\n        time_step=0.1,\n        max_iterations=1,\n    )\n    fem_area = fem_reference.area_list[0]\n    query_time = float(time_interval[1])\n    fem_area.sampling_area([80, 80])\n    fem_area.define_time(query_time)\n    fem_values = fem_area['u_ref']\n    pinn_area = domain.area_list[0].evaluate(model2_best)\n    u_error = float(np.mean(np.abs(pinn_area.data_dict['u'] - fem_values)))\n    if not np.isfinite(u_error):\n        raise RuntimeError('FEM comparison produced a non-finite error')\n    print(f'FEM mean absolute u error at t={query_time:g}: {u_error:.6e}')\n</code></pre>"},{"location":"heat_eq/#run-it-yourself","title":"Run it yourself","text":"<p>Source notebook: <code>examples/heat_eq/heat_eq.ipynb</code></p> <pre><code>jupyter notebook examples/heat_eq/heat_eq.ipynb\n</code></pre> <p>This page is hand-authored; the notebook is the source of truth (see REGENERATE.md).</p>"},{"location":"install/","title":"Installation","text":""},{"location":"install/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install deepflow\n</code></pre>"},{"location":"install/#optional-fem-reference-backend-deepflowcfd","title":"Optional: FEM reference backend (<code>deepflow[cfd]</code>)","text":"<p>The <code>[cfd]</code> extra installs NGSolve/Netgen, the finite-element backend that solves DeepFlow's built-in PDEs on unstructured meshes. You only need it if you want to compare PINN results against FEM reference solutions (see NGSolve reference solutions). It is optional and loaded lazily \u2014 a plain <code>import deepflow</code> never touches it:</p> <pre><code>pip install \"deepflow[cfd]\"\n</code></pre> <p>The extra requires Python 3.10+.</p>"},{"location":"install/#install-from-source","title":"Install from source","text":"<pre><code>git clone https://github.com/YoYo-XYZ/pinn-deepflow.git\ncd pinn-deepflow\npip install -e .\n</code></pre> <p>An editable install keeps the package in sync with the <code>src/</code> checkout \u2014 the right choice for development (see Contributing).</p>"},{"location":"install/#gpu-cuda","title":"GPU / CUDA","text":"<p>DeepFlow uses PyTorch as its backend and automatically selects CUDA when a compatible GPU is available (<code>df.device</code> reports <code>'cuda'</code>). To use it, install a PyTorch build with CUDA support first:</p> <pre><code>pip install torch --index-url https://download.pytorch.org/whl/cu121\n</code></pre> <p>Then verify device selection in Python:</p> <pre><code>import deepflow as df\nprint(df.device)  # 'cuda' or 'cpu'\n</code></pre>"},{"location":"install/#requirements","title":"Requirements","text":"<ul> <li>Python &gt;= 3.10</li> <li>PyTorch &gt;= 1.7.0</li> <li>NumPy &gt;= 1.19.0</li> <li>Matplotlib &gt;= 3.3.0</li> <li>SymPy &gt;= 1.5.0</li> <li>SciPy &gt;= 1.5.0</li> <li>ultraplot &gt;= 1.0.0</li> </ul>"},{"location":"install/#troubleshooting","title":"Troubleshooting","text":""},{"location":"install/#device-selection","title":"Device selection","text":"<p>DeepFlow selects <code>'cuda'</code> when <code>torch.cuda.is_available()</code> is true, <code>'cpu'</code> otherwise. To force the CPU:</p> <pre><code>import deepflow as df\ndf.device = 'cpu'\n</code></pre>"},{"location":"install/#expo_scaling-has-not-yet-defined-warning","title":"<code>expo_scaling has not yet defined</code> warning","text":"<p><code>define_time</code> without an explicit <code>expo_scaling</code> argument warns and defaults to <code>False</code>:</p> <pre><code>g.define_time(range_t=[0, 1], sampling_scheme='random', expo_scaling=False)\n</code></pre>"},{"location":"install/#import-errors-after-upgrading","title":"Import errors after upgrading","text":"<p>If <code>import deepflow</code> fails after an upgrade, the usual causes are a stale editable install or a broken PyTorch build. Reinstall cleanly:</p> <pre><code>pip uninstall deepflow\npip install deepflow\n</code></pre>"},{"location":"install/#missing-plots-or-visualization-failures","title":"Missing plots or visualization failures","text":"<p>The plotting layer is built on ultraplot. If plots fail with <code>ImportError: UltraPlot</code>, ensure <code>ultraplot &gt;= 1.0.0</code> is installed and not shadowed by a package named <code>UltraPlot</code> (the PyPI name is lowercase).</p>"},{"location":"quickstart/","title":"Quick Start","text":"<p>This example demonstrates how to simulate Steady channel flow under 20 lines of code! We recommend using a Python notebook (<code>.ipynb</code>) for interactive experience.</p>"},{"location":"quickstart/#1-define-the-geometry-and-physics","title":"1. Define the Geometry and Physics","text":"<p><pre><code>import deepflow as df\n\n# Define the area and bounds\nrectangle = df.geometry.rectangle([0, 5], [0, 1])\ndomain = df.domain(rectangle)\n\ndomain.show_setup() # Display the domain setup\n</code></pre> <pre><code># Define Boundary Conditions\ndomain.bound_list[0].define_bc({'u': 1, 'v': 0})  # Inflow: u=1\ndomain.bound_list[1].define_bc({'u': 0, 'v': 0})  # Wall: No slip\ndomain.bound_list[2].define_bc({'p': 0})          # Outflow: p=0\ndomain.bound_list[3].define_bc({'u': 0, 'v': 0})  # Wall: No slip\n\n# Define PDE (Navier-Stokes)\ndomain.area_list[0].define_pde(df.pde.NavierStokes(U=0.0001, L=1, mu=0.001, rho=1000))\n\ndomain.show_setup() # Display the domain setup\n</code></pre> </p> <p><pre><code># Sample points: [Left, Bottom, Right, Top], [Interior]\ndomain.sampling_random([200, 400, 200, 400], [5000])\ndomain.show_coordinates(display_physics=True)\n</code></pre> </p>"},{"location":"quickstart/#2-create-and-train-the-model","title":"2. Create and Train the model","text":"<p><pre><code># Initialize the PINN model\nmodel0 = df.PINN(width=40, length=4)\n</code></pre> <pre><code># Train the model using Adam Optimizer\nmodel1, model1_best = model0.train_adam(\n    calc_loss=df.calc_loss_simple(domain),\n    learning_rate=0.001,\n    epochs=2000,)\n</code></pre></p>"},{"location":"quickstart/#3-visualize-results","title":"3. Visualize Results","text":"<p><pre><code># Evaluate the best model\nprediction = domain.area_list[0].evaluate(model1_best)\nprediction.sampling_area([500, 100])\n\n# Plot Velocity Field\n_ = prediction.plot_color('u', cmap='jet')\n\n# Plot Training Loss\n_ =prediction.plot_loss_curve()\n</code></pre> </p>"},{"location":"quickstart/#4-optional-train-in-fp64-double-precision","title":"4. Optional: Train in FP64 (Double Precision)","text":"<p>Recent work on PINNs reports that double precision can improve convergence and accuracy. Enable FP64 globally before building the geometry or model:</p> <pre><code>import torch\nimport deepflow as df\n\ndf.dtype = torch.float64\n\n# Continue with geometry, PDE, sampling, and training as shown above.\n</code></pre> <p>Switch back to FP32 at any time with <code>df.dtype = torch.float32</code>.</p>"},{"location":"reference-solutions/","title":"NGSolve reference solutions","text":"<p>DeepFlow can optionally solve its built-in PDEs with an unstructured NGSolve/Netgen finite-element backend.  The dependency is not imported by a normal <code>import deepflow</code>.</p> <p>Install the optional extra in a supported Python 3.10+ environment:</p> <pre><code>pip install \"deepflow[cfd]\"\n</code></pre> <p>The backend accepts one connected two-dimensional PDE <code>Area</code>.  Rectangles, circles, polygons, lines, parameterized curves, and subtraction-defined holes are supported when their boundaries are represented by <code>Bound</code> objects.  An arbitrary <code>contains_fn</code> without explicit boundary curves cannot be meshed.</p> <p>Reference boundary conditions use the existing DeepFlow value syntax:</p> <pre><code>bound.define_bc({\"u\": 0})\nbound.define_bc({\"u\": (\"y\", lambda y: 4 * y * (1 - y))})\n</code></pre> <p>These are interpreted as Dirichlet values.  Hard constraints, derivative conditions such as <code>u_x</code>, and typed Neumann/traction/periodic conditions are reported as unsupported in v1.  Unspecified fields use the natural FEM condition.  A pressure field without a pressure Dirichlet condition is normalized to zero mean after each Navier\u2013Stokes solve; the normalization mode is recorded in <code>reference.metadata[\"pressure_gauge\"]</code>.</p>"},{"location":"reference-solutions/#usage","title":"Usage","text":"<p>The primary FEM workflow returns a <code>ReferenceGroupEvaluator</code>. Its per-geometry evaluators re-query the FEM fields, and point queries at arbitrary coordinates go through the underlying <code>ReferenceSolution</code>:</p> <pre><code>import numpy as np\nimport deepflow as df\n\narea = df.geometry.rectangle([0, 1], [0, 1])\ndomain = df.domain(area)\nfor bound in domain.bound_list:\n    bound.define_bc({\"u\": 0, \"v\": 0})\narea.define_pde(df.NavierStokes(mu=1.0, rho=1.0))\n\nreference = domain.solve_fem(\n    mesh_size=0.05,\n    boundary_resolution=128,\n)\nprint(reference.metadata)\n\n# Per-geometry FEM evaluation and visualization.\narea_eval = reference.area_list[0]\narea_eval.sampling_area([300, 150])\narea_eval.plot_color(\"u_ref\")\n\n# FEM data is returned by point query.\nx, y = np.meshgrid(\n    np.linspace(0.0, 1.0, 300),\n    np.linspace(0.0, 1.0, 150),\n    indexing=\"ij\",\n)\nu = reference.reference_solution.evaluate(x, y, fields=(\"u\",))[\"u\"]\n</code></pre> <p>Heat, wave, and transient Navier\u2013Stokes problems require a time interval and initial data.  Set <code>time_step</code> to control the stored snapshots; the default stores 100 uniform steps.  Transient <code>evaluate</code> calls linearly interpolate between snapshots.  <code>BurgersEquation1D</code> is treated as a steady 2D equation in the <code>(x, y)</code> domain and does not require a time interval.</p> <p>The returned <code>ReferenceGroupEvaluator</code> is a <code>GroupEvaluator</code> whose children re-query the FEM fields; the underlying <code>ReferenceSolution</code> (<code>.reference_solution</code>) supports point queries and export:</p> <pre><code>values = reference.reference_solution.evaluate(np.array([0.25, 0.5]), np.array([0.5, 0.5]))\nreference.reference_solution.export_npz(\"reference.npz\", x=[0.25, 0.5], y=[0.5, 0.5])\n</code></pre> <p><code>ReferenceSolver(...).solve(domain)</code> remains available as a lower-level alternative. The reference object retains the FEM fields in memory, so repeated queries do not solve the PDE again. <code>metadata</code> contains mesh statistics, iteration counts, residual diagnostics, any time values, and convergence status.</p>"},{"location":"reference/","title":"API Reference","text":"<p>This reference is generated from the public Google-style docstrings in the DeepFlow source code. Private implementation helpers are intentionally omitted.</p>"},{"location":"reference/#geometry","title":"Geometry","text":"<p>Two-dimensional geometry primitives and sampling helpers.</p>"},{"location":"reference/#deepflow.geometry.CustomData","title":"<code>CustomData(data_dict: Dict[str, Any])</code>","text":"<p>               Bases: <code>PhysicsAttach</code></p> <p>Attach PINN physics and evaluation to a fixed point cloud.</p> <p>Parameters:</p> Name Type Description Default <code>data_dict</code> <code>Dict[str, Any]</code> <p>Mapping containing <code>x</code> and <code>y</code> coordinate tensors.</p> required Notes <p>Custom data is not resampled by area or line samplers. Its coordinates are processed as supplied.</p> <p>Initialize a custom-data geometry from coordinate arrays.</p>"},{"location":"reference/#deepflow.geometry.Bound","title":"<code>Bound(range_val: List[float], *func: Callable, ref_axis: str = 'x')</code>","text":"<p>               Bases: <code>PhysicsAttach</code></p> <p>Represent a one-dimensional boundary in a two-dimensional space.</p> <p>A boundary is described by a reference-axis interval and one or more functions for the dependent coordinates. Use <code>line</code>, <code>curve</code>, or <code>point</code> for common constructions.</p> <p>Parameters:</p> Name Type Description Default <code>range_val</code> <code>List[float]</code> <p>Two-element interval on the reference axis.</p> required <code>*func</code> <code>Callable</code> <p>Functions mapping the reference coordinate to dependent coordinates, or two functions mapping a parameter to <code>x</code> and <code>y</code> when <code>ref_axis=\"t\"</code>.</p> <code>()</code> <code>ref_axis</code> <code>str</code> <p>Reference axis: <code>\"x\"</code>, <code>\"y\"</code>, or <code>\"t\"</code>.</p> <code>'x'</code> <p>Attributes:</p> Name Type Description <code>ranges</code> <code>Dict[int, List[float]]</code> <p>Minimum and maximum values for each coordinate axis.</p> <code>funcs</code> <code>Dict[int, List[Callable]]</code> <p>Parameterization functions keyed by reference axis.</p> <p>Initialize a boundary parameterization.</p>"},{"location":"reference/#deepflow.geometry.Bound.define_func","title":"<code>define_func(range_val: List[float], *func: Callable, ref_axis: str = 'y')</code>","text":"<p>Define or replace a dependent-coordinate parameterization.</p> <p>Parameters:</p> Name Type Description Default <code>range_val</code> <code>List[float]</code> <p>Interval for the selected reference axis.</p> required <code>*func</code> <code>Callable</code> <p>Functions defining dependent coordinates or a parametric <code>x(t), y(t)</code> curve.</p> <code>()</code> <code>ref_axis</code> <code>str</code> <p>Reference axis: <code>\"x\"</code>, <code>\"y\"</code>, or <code>\"t\"</code>.</p> <code>'y'</code> <p>Returns:</p> Type Description <p>None. The boundary bounds and sampled preview are updated in place.</p>"},{"location":"reference/#deepflow.geometry.Bound.sampling_line","title":"<code>sampling_line(n_points: int, scheme='uniform') -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Sample points along the boundary.</p> <p>Parameters:</p> Name Type Description Default <code>n_points</code> <code>int</code> <p>Number of points to generate.</p> required <code>scheme</code> <p>Sampling scheme: <code>\"uniform\"</code>, <code>\"random\"</code>, or <code>\"lhs\"</code> for Latin Hypercube Sampling.</p> <code>'uniform'</code> <p>Returns:</p> Type Description <code>Tuple[Tensor, Tensor]</code> <p>Tuple of one-dimensional <code>x</code> and <code>y</code> tensors.</p>"},{"location":"reference/#deepflow.geometry.Bound.mask_area","title":"<code>mask_area(*x: torch.Tensor) -&gt; torch.Tensor</code>","text":"<p>Return the mask of points rejected by this boundary.</p> <p>Parameters:</p> Name Type Description Default <code>*x</code> <code>Tensor</code> <p>Coordinate tensors in <code>x</code>/<code>y</code> axis order.</p> <code>()</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Boolean tensor with the same shape as the coordinate tensors.</p>"},{"location":"reference/#deepflow.geometry.Bound.show","title":"<code>show()</code>","text":"<p>Plot a sampled representation of the boundary.</p>"},{"location":"reference/#deepflow.geometry.Area","title":"<code>Area(bound_list: List[Bound], bounds_negative: Optional[List[Bound]] = None, *, contains_fn: Optional[Callable[[torch.Tensor, torch.Tensor], torch.Tensor]] = None, ranges: Optional[Dict[int, Tuple[float, float]]] = None)</code>","text":"<p>               Bases: <code>PhysicsAttach</code></p> <p>Represent a two-dimensional area bounded by <code>Bound</code> objects.</p> <p>Parameters:</p> Name Type Description Default <code>bound_list</code> <code>List[Bound]</code> <p>Boundary objects describing the exterior of the area.</p> required <code>bounds_negative</code> <code>Optional[List[Bound]]</code> <p>Optional boundaries describing holes to exclude.</p> <code>None</code> <code>contains_fn</code> <code>Optional[Callable[[Tensor, Tensor], Tensor]]</code> <p>Optional custom point-containment function.</p> <code>None</code> <code>ranges</code> <code>Optional[Dict[int, Tuple[float, float]]]</code> <p>Optional explicit <code>{axis: (lower, upper)}</code> bounding ranges.</p> <code>None</code> Notes <p>Areas support subtraction and union with <code>-</code>, <code>|</code>, and <code>+</code>. <code>sampling_area</code> filters candidate points through <code>contains</code>.</p> <p>Initialize an area from its boundary and containment definition.</p>"},{"location":"reference/#deepflow.geometry.Area.area_list","title":"<code>area_list: List[Area]</code>  <code>property</code>","text":"<p>Returns the list of Area objects in the domain.</p>"},{"location":"reference/#deepflow.geometry.Area.checkbound","title":"<code>checkbound()</code>","text":"<p>Determine each boundary's interior-facing direction.</p> <p>The direction is inferred using a ray-casting check from the area center and stored on each boundary as <code>reject_above</code>.</p> <p>Returns:</p> Type Description <p>None. Boundary objects are updated in place.</p>"},{"location":"reference/#deepflow.geometry.Area.contains","title":"<code>contains(x: torch.Tensor, y: torch.Tensor) -&gt; torch.Tensor</code>","text":"<p>Return a boolean mask selecting points inside or on the area.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Floating-point tensor of x coordinates.</p> required <code>y</code> <code>Tensor</code> <p>Floating-point tensor of y coordinates with the same shape, device, and dtype as <code>x</code>.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Boolean tensor indicating whether each point belongs to the area.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If coordinates are not compatible floating-point tensors or a custom containment function returns an invalid value.</p> <code>ValueError</code> <p>If coordinate shapes, devices, or dtypes differ.</p>"},{"location":"reference/#deepflow.geometry.Area.sampling_area","title":"<code>sampling_area(n_points_square: Union[int, List[int]], scheme='uniform') -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Sample and retain points inside the area.</p> <p>Parameters:</p> Name Type Description Default <code>n_points_square</code> <code>Union[int, List[int]]</code> <p>For uniform sampling, an integer gives the number of points per axis; for random and LHS sampling, it gives the number of candidate points. <code>[nx, ny]</code> supplies separate grid resolutions and generates <code>nx * ny</code> candidates.</p> required <code>scheme</code> <p>Sampling scheme: <code>\"uniform\"</code>, <code>\"random\"</code>, or <code>\"lhs\"</code>.</p> <code>'uniform'</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Tuple of sampled <code>x</code> and <code>y</code> tensors. Candidate points outside</p> <code>Tensor</code> <p>the area are removed.</p>"},{"location":"reference/#deepflow.geometry.Area.sampling_lines","title":"<code>sampling_lines(*n_points_per_line, scheme='random') -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Sample every boundary in the area.</p> <p>Parameters:</p> Name Type Description Default <code>*n_points_per_line</code> <p>One resolution applied to every boundary. If one value per boundary is supplied, all values must match so the result can be returned as dense stacked tensors.</p> <code>()</code> <code>scheme</code> <p>Sampling scheme passed to <code>Bound.sampling_line</code>.</p> <code>'random'</code> <p>Returns:</p> Type Description <code>Tuple[Tensor, Tensor]</code> <p>Stacked <code>x</code> and <code>y</code> tensors containing the boundary samples.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no resolution is supplied, the number of resolutions does not match the number of boundaries, or resolutions differ between boundaries.</p>"},{"location":"reference/#deepflow.geometry.Area.show","title":"<code>show(show_index: bool = False)</code>","text":"<p>Plot the boundary and a small interior sample of the area.</p> <p>Parameters:</p> Name Type Description Default <code>show_index</code> <code>bool</code> <p>Label exterior boundaries with their list indices.</p> <code>False</code>"},{"location":"reference/#deepflow.geometry.custom_data","title":"<code>custom_data(data_dict: Dict[str, Any]) -&gt; CustomData</code>","text":"<p>Create a geometry backed by user-provided coordinate data.</p> <p>Parameters:</p> Name Type Description Default <code>data_dict</code> <code>Dict[str, Any]</code> <p>Mapping containing at least <code>x</code> and <code>y</code> coordinate tensors. Additional entries are retained as user data.</p> required <p>Returns:</p> Type Description <code>CustomData</code> <p>A <code>CustomData</code> instance representing the supplied points.</p>"},{"location":"reference/#deepflow.geometry.circle","title":"<code>circle(x: float, y: float, r: float) -&gt; Area</code>","text":"<p>Create a circular area.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>Center x coordinate.</p> required <code>y</code> <code>float</code> <p>Center y coordinate.</p> required <code>r</code> <code>float</code> <p>Positive radius.</p> required <p>Returns:</p> Type Description <code>Area</code> <p>A circular <code>Area</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>r</code> is not positive.</p>"},{"location":"reference/#deepflow.geometry.rectangle","title":"<code>rectangle(x_range: List[float], y_range: List[float]) -&gt; Area</code>","text":"<p>Create an axis-aligned rectangular area.</p> <p>Parameters:</p> Name Type Description Default <code>x_range</code> <code>List[float]</code> <p>Two-element x interval.</p> required <code>y_range</code> <code>List[float]</code> <p>Two-element y interval.</p> required <p>Returns:</p> Type Description <code>Area</code> <p>A rectangular <code>Area</code>.</p>"},{"location":"reference/#deepflow.geometry.line_horizontal","title":"<code>line_horizontal(y: float, range_x: List[float]) -&gt; Bound</code>","text":"<p>Create a horizontal boundary at a fixed y coordinate.</p>"},{"location":"reference/#deepflow.geometry.line_vertical","title":"<code>line_vertical(x: float, range_y: List[float]) -&gt; Bound</code>","text":"<p>Create a vertical boundary at a fixed x coordinate.</p>"},{"location":"reference/#deepflow.geometry.line","title":"<code>line(pos1: List[float], pos2: List[float]) -&gt; Bound</code>","text":"<p>Create a straight boundary between two points.</p> <p>Parameters:</p> Name Type Description Default <code>pos1</code> <code>List[float]</code> <p>First <code>[x, y]</code> endpoint.</p> required <code>pos2</code> <code>List[float]</code> <p>Second <code>[x, y]</code> endpoint.</p> required <p>Returns:</p> Type Description <code>Bound</code> <p>A <code>Bound</code> representing the line segment.</p>"},{"location":"reference/#deepflow.geometry.polygon","title":"<code>polygon(*pos: List[float]) -&gt; Area</code>","text":"<p>Create a polygon from ordered vertex coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>*pos</code> <code>List[float]</code> <p>At least three <code>[x, y]</code> vertices in clockwise or counter-clockwise order.</p> <code>()</code> <p>Returns:</p> Type Description <code>Area</code> <p>A polygonal <code>Area</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If fewer than three vertices are supplied, vertices do not have shape <code>(n, 2)</code>, or a coordinate is non-finite.</p>"},{"location":"reference/#deepflow.geometry.curve","title":"<code>curve(range_val: List[float], *func, ref_axis='x') -&gt; Bound</code>","text":"<p>Create a parameterized boundary from coordinate functions.</p> <p>Parameters:</p> Name Type Description Default <code>range_val</code> <code>List[float]</code> <p>Interval for the reference axis or parameter.</p> required <code>*func</code> <p>Boundary functions accepted by <code>Bound</code>.</p> <code>()</code> <code>ref_axis</code> <p>Reference axis: <code>\"x\"</code>, <code>\"y\"</code>, or <code>\"t\"</code>.</p> <code>'x'</code> <p>Returns:</p> Type Description <code>Bound</code> <p>A parameterized <code>Bound</code>.</p>"},{"location":"reference/#deepflow.geometry.point","title":"<code>point(x, y) -&gt; Bound</code>","text":"<p>Create a point-like boundary at <code>(x, y)</code>.</p>"},{"location":"reference/#domains-and-losses","title":"Domains and Losses","text":"<p>Problem-domain orchestration, sampling, and PINN loss construction.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain","title":"<code>ProblemDomain(bound_list: list[Bound], area_list: list[Area])</code>","text":"<p>Collect geometries and coordinate the PINN workflow.</p> <p>Parameters:</p> Name Type Description Default <code>bound_list</code> <code>list[Bound]</code> <p>Boundary geometries, including area boundaries.</p> required <code>area_list</code> <code>list[Area]</code> <p>Areas or custom-data geometries.</p> required <p>Attributes:</p> Name Type Description <code>bound_list</code> <p>Boundaries in domain order.</p> <code>area_list</code> <p>Areas in domain order.</p> <code>sampling_option</code> <p>Name of the most recent sampling strategy.</p> Notes <p>Domain sampling methods update geometry coordinates in place. Call <code>evaluate</code> only after every included geometry has coordinates.</p> <p>Initialize a domain from its boundary and area lists.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.evaluate","title":"<code>evaluate(model)</code>","text":"<p>Create evaluators for every unique sampled geometry.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <p>PINN model used to compute predictions and residuals.</p> required <p>Returns:</p> Type Description <p>A <code>deepflow.evaluation.GroupEvaluator</code> containing one child</p> <p>evaluator per unique geometry.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an included geometry has not been sampled.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.solve_fem","title":"<code>solve_fem(mesh_size=None, boundary_resolution=128, time_step=None, tolerance=1e-08, max_iterations=200) -&gt; ReferenceGroupEvaluator</code>","text":"<p>Solve the domain with the optional NGSolve FEM backend.</p> <p>Parameters:</p> Name Type Description Default <code>mesh_size</code> <p>Target FEM mesh size. If <code>None</code>, infer it from the PDE area extent.</p> <code>None</code> <code>boundary_resolution</code> <p>Number of samples used to construct boundary curves for the mesh.</p> <code>128</code> <code>time_step</code> <p>Time spacing for transient reference snapshots.</p> <code>None</code> <code>tolerance</code> <p>Nonlinear or iterative solver convergence tolerance.</p> <code>1e-08</code> <code>max_iterations</code> <p>Maximum number of solver iterations.</p> <code>200</code> <p>Returns:</p> Type Description <code>ReferenceGroupEvaluator</code> <p>A <code>ReferenceGroupEvaluator</code> whose children query the solved FEM</p> <code>ReferenceGroupEvaluator</code> <p>fields. The underlying <code>ReferenceSolution</code> is available</p> <code>ReferenceGroupEvaluator</code> <p>as <code>.reference_solution</code>.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If the optional NGSolve backend is unavailable.</p> <code>ValueError</code> <p>If the domain or solver configuration is invalid.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_uniform","title":"<code>sampling_uniform(bound_sampling_res: list = [], area_sampling_res: list = [])</code>","text":"<p>Sample boundaries and areas on uniform grids.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Resolution for each entry in <code>bound_list</code>.</p> <code>[]</code> <code>area_sampling_res</code> <code>list</code> <p>Resolution for each entry in <code>area_list</code>.</p> <code>[]</code> <p>Returns:</p> Type Description <p>None. Geometry coordinates are replaced in place.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_random","title":"<code>sampling_random(bound_sampling_res: list = [], area_sampling_res: list = [])</code>","text":"<p>Sample boundaries and areas with uniform random points.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Number of points for each boundary.</p> <code>[]</code> <code>area_sampling_res</code> <code>list</code> <p>Number of candidate points for each area.</p> <code>[]</code> <p>Returns:</p> Type Description <p>None. Geometry coordinates are replaced in place.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_lhs","title":"<code>sampling_lhs(bound_sampling_res: list = [], area_sampling_res: list = [])</code>","text":"<p>Sample boundaries and areas with Latin Hypercube Sampling.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Number of points for each boundary.</p> <code>[]</code> <code>area_sampling_res</code> <code>list</code> <p>Number of candidate points for each area.</p> <code>[]</code> <p>Returns:</p> Type Description <p>None. Geometry coordinates are replaced in place.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_RAR","title":"<code>sampling_RAR(bound_top_k_list: list = None, area_top_k_list: list = None, bound_candidates_num_list: list = None, area_candidates_num_list: list = None)</code>","text":"<p>Add residual-adaptive points to the existing training samples.</p> <p>Parameters:</p> Name Type Description Default <code>bound_top_k_list</code> <code>list</code> <p>Number of high-residual points to retain per boundary.</p> <code>None</code> <code>area_top_k_list</code> <code>list</code> <p>Number of high-residual points to retain per area.</p> <code>None</code> <code>bound_candidates_num_list</code> <code>list</code> <p>Candidate counts per boundary.</p> <code>None</code> <code>area_candidates_num_list</code> <code>list</code> <p>Candidate counts per area.</p> <code>None</code> <p>Returns:</p> Type Description <p>None. Selected points are appended to geometry coordinates.</p> Notes <p>Residual fields must have been computed before calling this method.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_R3","title":"<code>sampling_R3(bound_sampling_res: list = None, area_sampling_res: list = None)</code>","text":"<p>R3 residual-based resampling with a fixed total budget (paper version).</p> <p>For each geometry, points whose residual exceeds the mean are kept, and the remaining budget is filled with fresh random samples. If the threshold set is larger than the budget, the highest-residual points are retained instead. The total number of points per geometry therefore stays equal to the requested resolution over time.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Fixed total point budget per boundary.</p> <code>None</code> <code>area_sampling_res</code> <code>list</code> <p>Fixed total point budget per area.</p> <code>None</code> <p>Returns:</p> Type Description <p>None. Geometry coordinates are resampled in place.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_R3_","title":"<code>sampling_R3_(bound_sampling_res: list = None, area_sampling_res: list = None)</code>","text":"<p>R3 residual-based resampling with an accumulating budget.</p> <p>For each geometry, points whose residual exceeds the mean are kept and added on top of a fresh random sample of size <code>res</code>. The total number of points therefore grows over time. This matches the historical deepflow behaviour prior to the paper-correct <code>sampling_R3</code> change.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Number of fresh candidate points per boundary.</p> <code>None</code> <code>area_sampling_res</code> <code>list</code> <p>Number of fresh candidate points per area.</p> <code>None</code> <p>Returns:</p> Type Description <p>None. Geometry point counts may grow after each call.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.sampling_accumulate","title":"<code>sampling_accumulate(bound_sampling_res: list = None, area_sampling_res: list = None)</code>","text":"<p>Accumulate residual-based points across repeated sampling calls.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Number of fresh points per boundary.</p> <code>None</code> <code>area_sampling_res</code> <code>list</code> <p>Number of fresh points per area.</p> <code>None</code> <p>Returns:</p> Type Description <p>None. This experimental method grows the training point set.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.clear_residual_points","title":"<code>clear_residual_points()</code>","text":"<p>Discard residual-based point buffers from all geometries.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.save_coordinates","title":"<code>save_coordinates()</code>","text":"<p>Save a copy of every geometry's current coordinates.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.load_coordinates","title":"<code>load_coordinates()</code>","text":"<p>Restore coordinates previously saved by <code>save_coordinates</code>.</p>"},{"location":"reference/#deepflow.domain.ProblemDomain.show_coordinates","title":"<code>show_coordinates(display_physics=False, xlim=None, ylim=None, display_resampling=False)</code>","text":"<p>Plot the currently sampled coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>display_physics</code> <p>Annotate geometries with attached BC, IC, or PDE information.</p> <code>False</code> <code>xlim</code> <p>Optional x-axis limits.</p> <code>None</code> <code>ylim</code> <p>Optional y-axis limits.</p> <code>None</code> <code>display_resampling</code> <p>Show buffered residual-based points.</p> <code>False</code>"},{"location":"reference/#deepflow.domain.ProblemDomain.show_setup","title":"<code>show_setup(bound_sampling_res: list = None, area_sampling_res: list = None, xlim=None, ylim=None)</code>","text":"<p>Plot the geometry setup and a temporary sampling preview.</p> <p>Parameters:</p> Name Type Description Default <code>bound_sampling_res</code> <code>list</code> <p>Optional preview resolution per boundary.</p> <code>None</code> <code>area_sampling_res</code> <code>list</code> <p>Optional preview resolution per area.</p> <code>None</code> <code>xlim</code> <p>Optional x-axis limits.</p> <code>None</code> <code>ylim</code> <p>Optional y-axis limits.</p> <code>None</code>"},{"location":"reference/#deepflow.domain.domain","title":"<code>domain(*geometries)</code>","text":"<p>Build a <code>ProblemDomain</code> from geometry objects.</p> <p>Parameters:</p> Name Type Description Default <code>*geometries</code> <p>Individual <code>Bound</code>, <code>Area</code>, or <code>CustomData</code> objects, or homogeneous lists of bounds or areas.</p> <code>()</code> <p>Returns:</p> Type Description <p>A problem domain containing separate boundary and area lists.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If an argument is not a supported geometry or a list mixes geometry types.</p>"},{"location":"reference/#deepflow.domain.calc_loss_simple","title":"<code>calc_loss_simple(domain: ProblemDomain) -&gt; callable</code>","text":"<p>Create an unweighted PINN loss function for a domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>ProblemDomain</code> <p>Domain whose boundary, initial, and PDE residuals are evaluated.</p> required <p>Returns:</p> Type Description <code>callable</code> <p>Callable accepting a model and returning a dictionary containing</p> <code>callable</code> <p><code>bc_loss</code>, <code>ic_loss</code>, <code>pde_loss</code>, and <code>total_loss</code>.</p>"},{"location":"reference/#deepflow.domain.calc_loss_weighted","title":"<code>calc_loss_weighted(domain: ProblemDomain, bc_weights=1, ic_weights=1, pde_weights=1) -&gt; callable</code>","text":"<p>Create a weighted PINN loss function for a domain.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>ProblemDomain</code> <p>Domain whose residuals are evaluated.</p> required <code>bc_weights</code> <p>Weight applied to boundary-condition loss.</p> <code>1</code> <code>ic_weights</code> <p>Weight applied to initial-condition loss.</p> <code>1</code> <code>pde_weights</code> <p>Weight applied to PDE loss.</p> <code>1</code> <p>Returns:</p> Type Description <code>callable</code> <p>Callable accepting a model and returning the weighted loss dictionary.</p>"},{"location":"reference/#physics-attachments","title":"Physics Attachments","text":"<p>Physics constraints and coordinate processing attached to geometries.</p>"},{"location":"reference/#deepflow.physicsinformed.func","title":"<code>func = function</code>  <code>module-attribute</code>","text":""},{"location":"reference/#deepflow.physicsinformed.parabolic","title":"<code>parabolic = parabolic_func</code>  <code>module-attribute</code>","text":""},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach","title":"<code>PhysicsAttach()</code>","text":"<p>Manage constraints, residuals, and samples for a PINN geometry.</p> <p>The class is inherited by geometry objects and normally used through methods such as <code>define_bc</code>, <code>define_pde</code>, and <code>process_coordinates</code>.</p> <p>Attributes:</p> Name Type Description <code>physics_type</code> <code>Optional[list[str]]</code> <p>Attached physics kind: <code>\"BC\"</code>, <code>\"IC\"</code>, or <code>\"PDE\"</code>.</p> <code>inputs_tensor_dict</code> <code>Dict[str, Optional[Tensor]]</code> <p>Coordinate tensors used by the model.</p> <code>model_outputs</code> <code>Dict</code> <p>Most recently cached model predictions.</p> <code>residual_field</code> <code>Dict</code> <p>Pointwise absolute residual magnitude.</p> <p>Initialize empty physics and coordinate state.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.define_bc","title":"<code>define_bc(condition_dict: Dict[str, Any]) -&gt; None</code>","text":"<p>Define Boundary Conditions (BC).</p> <p>Parameters:</p> Name Type Description Default <code>condition_dict</code> <code>Dict[str, Any]</code> <p>Dictionary mapping variable names to conditions.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None. The geometry is marked as a boundary-condition geometry.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.define_ic","title":"<code>define_ic(condition_dict: Dict[str, Any]) -&gt; None</code>","text":"<p>Define Initial Conditions (IC).</p> <p>Parameters:</p> Name Type Description Default <code>condition_dict</code> <code>Dict[str, Any]</code> <p>Dictionary mapping variable names to conditions.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None. The geometry is marked as an initial-condition geometry.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.define_pde","title":"<code>define_pde(pde: PDE) -&gt; None</code>","text":"<p>Define the Partial Differential Equation (PDE) to enforce.</p> <p>Parameters:</p> Name Type Description Default <code>pde</code> <code>PDE</code> <p>Initialized <code>deepflow.pde.PDE</code> instance.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None. The geometry is marked as a PDE geometry.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.set_coordinates","title":"<code>set_coordinates(x: torch.Tensor, y: torch.Tensor) -&gt; None</code>","text":"<p>Set spatial coordinates manually.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Tensor of x coordinates.</p> required <code>y</code> <code>Tensor</code> <p>Tensor of y coordinates with the same shape as <code>x</code>.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None. Coordinates are stored until <code>process_coordinates</code> is</p> <code>None</code> <p>called.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.define_time","title":"<code>define_time(range_t: Union[Tuple[float, float], int, float] = None, sampling_scheme: str = None, expo_scaling=None) -&gt; None</code>","text":"<p>Configure the time coordinate range and sampling scheme.</p> <p>Parameters:</p> Name Type Description Default <code>range_t</code> <code>Union[Tuple[float, float], int, float]</code> <p>A <code>(start, end)</code> interval or a fixed time value.</p> <code>None</code> <code>sampling_scheme</code> <code>str</code> <p><code>\"uniform\"</code> or <code>\"random\"</code> for intervals.</p> <code>None</code> <code>expo_scaling</code> <p>Whether to apply exponential time scaling.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no range is supplied and no previous range exists.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.sampling_time","title":"<code>sampling_time() -&gt; None</code>","text":"<p>Generate time coordinates from the configured time range.</p> <p>Returns:</p> Type Description <code>None</code> <p>None. The generated values are stored in <code>t</code> and <code>T</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If spatial coordinates or a time range are missing.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.process_coordinates","title":"<code>process_coordinates(device: Optional[torch.device] = None) -&gt; Dict[str, Optional[torch.Tensor]]</code>","text":"<p>Prepare coordinate tensors for PINN training.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>Optional[device]</code> <p>Target device. If <code>None</code>, use DeepFlow's configured device.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, Optional[Tensor]]</code> <p>Dictionary of differentiable model-input tensors.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If spatial coordinates are not set.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.calc_output","title":"<code>calc_output() -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Post-process cached model outputs to match target conditions. Handles derivative constraints (e.g., if key is 'u_x').</p> <p>Uses <code>self.model_inputs</code> (not <code>self.inputs_tensor_dict</code>) for derivative computation so that batched forward passes work correctly: <code>model_inputs</code> points to the tensors actually fed to the model (which are in the autograd graph), whereas <code>inputs_tensor_dict</code> may hold the original per-geometry tensors that are not in a batched graph.</p> <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Predictions or derivatives corresponding to the configured target</p> <code>Dict[str, Tensor]</code> <p>conditions.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.calc_loss","title":"<code>calc_loss(model: nn.Module) -&gt; torch.Tensor</code>","text":"<p>Calculate the mean per-point sum of squared residual components.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Model used to produce predictions.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Scalar loss tensor.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.calc_residual_field","title":"<code>calc_residual_field(model: nn.Module) -&gt; Union[int, torch.Tensor]</code>","text":"<p>Calculate the pointwise absolute residual field.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Model used to produce predictions.</p> required <p>Returns:</p> Type Description <code>Union[int, Tensor]</code> <p>One-dimensional tensor containing the absolute residual magnitude</p> <code>Union[int, Tensor]</code> <p>at each sample point.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.set_threshold","title":"<code>set_threshold(loss: float = None, top_k_loss: float = None) -&gt; None</code>","text":"<p>Set thresholds used by adaptive sampling or convergence checks.</p> <p>Parameters:</p> Name Type Description Default <code>loss</code> <code>float</code> <p>Mean-loss threshold.</p> <code>None</code> <code>top_k_loss</code> <code>float</code> <p>Threshold for selecting high-residual points.</p> <code>None</code>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.save_coordinates","title":"<code>save_coordinates() -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Save the current spatial coordinates for later restoration.</p> <p>Returns:</p> Type Description <code>Tuple[Tensor, Tensor]</code> <p>Tuple of saved x and y tensors.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.get_residual_based_points_topk","title":"<code>get_residual_based_points_topk(top_k: int) -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Select points with the largest residual magnitudes.</p> <p>Parameters:</p> Name Type Description Default <code>top_k</code> <code>int</code> <p>Number of points to select.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Tuple of selected x and y tensors. The points are also appended to</p> <code>Tensor</code> <p>the residual-point buffers.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.get_residual_based_points_threshold","title":"<code>get_residual_based_points_threshold(threshold: float = None, maintain_points=False) -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Select points whose residual exceeds a threshold.</p> <p>Parameters:</p> Name Type Description Default <code>threshold</code> <code>float</code> <p>Residual threshold. If <code>None</code>, use the field mean.</p> <code>None</code> <code>maintain_points</code> <p>Avoid re-adding points already selected during an accumulating resampling cycle.</p> <code>False</code> <p>Returns:</p> Type Description <code>Tuple[Tensor, Tensor]</code> <p>Tuple of selected x and y tensors stored in the residual buffers.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.apply_residual_based_points","title":"<code>apply_residual_based_points() -&gt; None</code>","text":"<p>Incorporate residual-based sampled points into the training set.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.clear_residual_based_points","title":"<code>clear_residual_based_points() -&gt; None</code>","text":"<p>Clear buffered residual-based points.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.process_model","title":"<code>process_model(model: nn.Module) -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Run a model on the processed inputs and cache its outputs.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Model accepting <code>inputs_tensor_dict</code>.</p> required <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary of model output tensors.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.process_pde","title":"<code>process_pde() -&gt; None</code>","text":"<p>Compute PDE residuals from the cached inputs and outputs.</p>"},{"location":"reference/#deepflow.physicsinformed.PhysicsAttach.evaluate","title":"<code>evaluate(model: nn.Module)</code>","text":"<p>Create an evaluator for this geometry and model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>PINN model used for prediction and residual evaluation.</p> required <p>Returns:</p> Type Description <p>A <code>deepflow.evaluation.Evaluator</code> instance.</p>"},{"location":"reference/#deepflow.physicsinformed.function","title":"<code>function(input_key: str, function: Callable) -&gt; List[Union[str, Callable]]</code>","text":"<p>Package a coordinate key and callable boundary condition.</p> <p>Parameters:</p> Name Type Description Default <code>input_key</code> <code>str</code> <p>Coordinate name passed to <code>function</code>.</p> required <code>function</code> <code>Callable</code> <p>Callable producing the target value.</p> required <p>Returns:</p> Type Description <code>List[Union[str, Callable]]</code> <p>Two-item list <code>[input_key, function]</code> accepted by <code>define_bc</code> or</p> <code>List[Union[str, Callable]]</code> <p><code>define_ic</code>.</p>"},{"location":"reference/#deepflow.physicsinformed.parabolic_func","title":"<code>parabolic_func(input_key: str, width, max_val, center_distance=0) -&gt; List[Union[str, Callable]]</code>","text":"<p>Create a parabolic boundary-condition function descriptor.</p> <p>Parameters:</p> Name Type Description Default <code>input_key</code> <code>str</code> <p>Coordinate name used by the generated callable.</p> required <code>width</code> <p>Width parameter of the parabola.</p> required <code>max_val</code> <p>Value at the parabola center.</p> required <code>center_distance</code> <p>Coordinate of the center.</p> <code>0</code> <p>Returns:</p> Type Description <code>List[Union[str, Callable]]</code> <p>Two-item list compatible with <code>function</code>.</p>"},{"location":"reference/#pdes","title":"PDEs","text":"<p>Built-in differential-equation residual models for PINN training.</p>"},{"location":"reference/#deepflow.pde.PDE","title":"<code>PDE()</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for physics-informed differential equations.</p> <p>Subclasses populate <code>residual_fields</code> in <code>compute_residuals</code>. Residual and loss helpers then reduce those fields over the sample points.</p> <p>Attributes:</p> Name Type Description <code>var</code> <p>Derived variables and residuals exposed during evaluation.</p> <code>residual_fields</code> <p>Tuple of per-equation residual tensors after a residual computation.</p>"},{"location":"reference/#deepflow.pde.PDE.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor, ...]</code>  <code>abstractmethod</code>","text":"<p>Compute and store equation residuals for model outputs.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Mapping containing coordinate tensors and predicted field tensors required by the PDE.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor, ...]</code> <p>Tuple of residual tensors, one for each equation.</p>"},{"location":"reference/#deepflow.pde.PDE.calc_residual_field","title":"<code>calc_residual_field() -&gt; torch.Tensor</code>","text":"<p>Return the pointwise sum of absolute residual components.</p>"},{"location":"reference/#deepflow.pde.PDE.calc_residual_field_raw","title":"<code>calc_residual_field_raw() -&gt; torch.Tensor</code>","text":"<p>Return residual components without taking absolute values.</p>"},{"location":"reference/#deepflow.pde.PDE.calc_residuals","title":"<code>calc_residuals() -&gt; torch.Tensor</code>","text":"<p>Return the mean pointwise sum of absolute residual components.</p>"},{"location":"reference/#deepflow.pde.PDE.calc_loss_field","title":"<code>calc_loss_field() -&gt; torch.Tensor</code>","text":"<p>Return the pointwise sum of squared residual components.</p>"},{"location":"reference/#deepflow.pde.PDE.calc_loss","title":"<code>calc_loss() -&gt; torch.Tensor</code>","text":"<p>Return the mean pointwise sum of squared residual components.</p>"},{"location":"reference/#deepflow.pde.CustomPDE","title":"<code>CustomPDE(func)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>Wrap a user-supplied residual function as a <code>PDE</code>.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <p>Callable accepting an input dictionary and returning a tuple of residual tensors.</p> required <p>Initialize a custom residual wrapper.</p>"},{"location":"reference/#deepflow.pde.CustomPDE.compute_residuals","title":"<code>compute_residuals(inputs_dict)</code>","text":"<p>Evaluate the user-supplied residual function.</p>"},{"location":"reference/#deepflow.pde.NavierStokes","title":"<code>NavierStokes(mu: float, rho: float, U: float = 1.0, L: float = 1.0)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>Incompressible Navier-Stokes equations (2D). Handles both Steady and Unsteady states automatically based on input 't'.</p> <p>Parameters:</p> Name Type Description Default <code>mu</code> <code>float</code> <p>Dynamic viscosity.</p> required <code>rho</code> <code>float</code> <p>Fluid density.</p> required <code>U</code> <code>float</code> <p>Reference velocity used for nondimensionalization.</p> <code>1.0</code> <code>L</code> <code>float</code> <p>Reference length used for nondimensionalization.</p> <code>1.0</code> <p>Initialize the nondimensionalized Navier-Stokes model.</p>"},{"location":"reference/#deepflow.pde.NavierStokes.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor, ...]</code>","text":"<p>Compute continuity and momentum residuals.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Must contain <code>x</code>, <code>y</code>, <code>u</code>, <code>v</code>, and <code>p</code>; include <code>t</code> for an unsteady problem.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor, ...]</code> <p>Tuple <code>(continuity, x_momentum, y_momentum)</code>.</p>"},{"location":"reference/#deepflow.pde.NavierStokes.nondimensionalize_inputs","title":"<code>nondimensionalize_inputs(inputs: Dict[str, torch.Tensor]) -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Scale recognized physical inputs to nondimensional form.</p> <p>Parameters:</p> Name Type Description Default <code>inputs</code> <code>Dict[str, Tensor]</code> <p>Mapping of coordinate and field names to tensors.</p> required <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>A new mapping with known quantities divided by their reference</p> <code>Dict[str, Tensor]</code> <p>scales; unknown entries are copied unchanged.</p>"},{"location":"reference/#deepflow.pde.StreamFunctionNavierStokes","title":"<code>StreamFunctionNavierStokes(mu: float, rho: float, U: float = 1.0, L: float = 1.0)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>Steady incompressible Navier-Stokes equations using <code>psi</code> and <code>p</code>.</p> <p>The velocity is derived from the stream function using <code>u = dpsi/dy</code> and <code>v = -dpsi/dx</code>.  Continuity is therefore satisfied identically, and the PDE contributes only the two momentum residuals. This formulation currently supports steady problems with <code>x</code> and <code>y</code> coordinates only.</p> <p>Parameters:</p> Name Type Description Default <code>mu</code> <code>float</code> <p>Dynamic viscosity.</p> required <code>rho</code> <code>float</code> <p>Fluid density.</p> required <code>U</code> <code>float</code> <p>Reference velocity used for nondimensionalization.</p> <code>1.0</code> <code>L</code> <code>float</code> <p>Reference length used for nondimensionalization.</p> <code>1.0</code> <p>Initialize the steady stream-function formulation.</p>"},{"location":"reference/#deepflow.pde.StreamFunctionNavierStokes.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor, ...]</code>","text":"<p>Compute the two momentum residuals from <code>psi</code> and <code>p</code>.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Must contain <code>x</code>, <code>y</code>, <code>psi</code>, and <code>p</code>.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor, ...]</code> <p>Tuple <code>(x_momentum, y_momentum)</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If transient coordinate <code>t</code> is supplied.</p>"},{"location":"reference/#deepflow.pde.StreamFunctionNavierStokes.nondimensionalize_inputs","title":"<code>nondimensionalize_inputs(inputs: Dict[str, torch.Tensor]) -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Scale recognized stream-function inputs to nondimensional form.</p> <p>Parameters:</p> Name Type Description Default <code>inputs</code> <code>Dict[str, Tensor]</code> <p>Mapping of coordinate and field names to tensors.</p> required <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>A new mapping with known quantities divided by their reference</p> <code>Dict[str, Tensor]</code> <p>scales.</p>"},{"location":"reference/#deepflow.pde.HeatEquation","title":"<code>HeatEquation(alpha: float)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>2D Heat Equation: u_t = alpha * (u_xx + u_yy)</p> <p>Parameters:</p> Name Type Description Default <code>alpha</code> <code>float</code> <p>Thermal diffusivity.</p> required <p>Initialize the heat-equation residual model.</p>"},{"location":"reference/#deepflow.pde.HeatEquation.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor]</code>","text":"<p>Compute the heat-equation residual.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Mapping containing <code>x</code>, <code>y</code>, <code>t</code>, and <code>u</code>.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor]</code> <p>A one-element tuple containing <code>u_t - alpha * (u_xx + u_yy)</code>.</p>"},{"location":"reference/#deepflow.pde.WaveEquation","title":"<code>WaveEquation(c: float)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>2D Wave Equation: u_tt = c^2 * (u_xx + u_yy)</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <code>float</code> <p>Wave propagation speed.</p> required <p>Initialize the wave-equation residual model.</p>"},{"location":"reference/#deepflow.pde.WaveEquation.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor]</code>","text":"<p>Compute the wave-equation residual.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Mapping containing <code>x</code>, <code>y</code>, <code>t</code>, and <code>u</code>.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor]</code> <p>A one-element tuple containing <code>u_tt - c**2 * (u_xx + u_yy)</code>.</p>"},{"location":"reference/#deepflow.pde.BurgersEquation1D","title":"<code>BurgersEquation1D(nu: float)</code>","text":"<p>               Bases: <code>PDE</code></p> <p>Steady 2D Burgers equation in the x-y domain: u_y + u * u_x = nu * u_xx.</p> <p>Parameters:</p> Name Type Description Default <code>nu</code> <code>float</code> <p>Viscosity coefficient.</p> required <p>Initialize the steady Burgers residual model.</p>"},{"location":"reference/#deepflow.pde.BurgersEquation1D.compute_residuals","title":"<code>compute_residuals(inputs_dict: Dict[str, torch.Tensor]) -&gt; Tuple[torch.Tensor]</code>","text":"<p>Compute the steady Burgers residual.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Mapping containing <code>x</code>, <code>y</code>, and <code>u</code>.</p> required <p>Returns:</p> Type Description <code>Tuple[Tensor]</code> <p>A one-element tuple containing <code>u_y + u * u_x - nu * u_xx</code>.</p>"},{"location":"reference/#neural-networks","title":"Neural Networks","text":"<p>Neural-network models and training helpers for PINNs.</p>"},{"location":"reference/#deepflow.nn.HardConstraint","title":"<code>HardConstraint(constant: float = 0.0)</code>","text":"<p>Represent a constant boundary or initial-condition constraint.</p> <p>Parameters:</p> Name Type Description Default <code>constant</code> <code>float</code> <p>Value imposed by the constraint.</p> <code>0.0</code> Notes <p>Use <code>hard_constraint</code> as a concise factory when defining a geometry condition.</p> <p>Initialize a constant hard constraint.</p>"},{"location":"reference/#deepflow.nn.HardConstraint.define_zero_func","title":"<code>define_zero_func(bound)</code>  <code>staticmethod</code>","text":"<p>Create a function that vanishes on a boundary.</p> <p>Parameters:</p> Name Type Description Default <code>bound</code> <p>Boundary whose parameterization defines the zero set.</p> required <p>Returns:</p> Type Description <p>Callable accepting a coordinate mapping and returning the signed</p> <p>distance-like constraint factor.</p>"},{"location":"reference/#deepflow.nn.NN","title":"<code>NN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>ABC</code>, <code>Module</code></p> <p>Physics-Informed Neural Network (PINN) model.</p> <p>A feedforward neural network that takes input coordinates (e.g., x, y, t) and outputs physical quantities (e.g., u, v, p), with support for  hard constraints.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs, such as <code>[\"x\", \"y\"]</code>.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields, such as <code>[\"u\", \"v\", \"p\"]</code>.</p> <code>None</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Initialization alias (<code>\"kaiming\"</code>/<code>\"he\"</code> or <code>\"xavier\"</code>/<code>\"glorot\"</code>), <code>None</code>, or a callable.</p> <code>'kaiming'</code> <p>Attributes:</p> Name Type Description <code>loss_history</code> <p>Recorded total, boundary, PDE, and optional initial condition losses.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>list</code> <p>List of input variable names (e.g., ['x', 'y']).</p> <code>None</code> <code>output_vars</code> <code>list</code> <p>List of output variable names (e.g., ['u']).</p> <code>None</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme for <code>nn.Linear</code> layers. Supported string aliases: <code>'kaiming'</code>/<code>'he'</code> (default), <code>'xavier'</code>/<code>'glorot'</code>.  <code>None</code> leaves PyTorch's default initialization untouched.  A callable receives the model and can apply arbitrary initialization.</p> <code>'kaiming'</code>"},{"location":"reference/#deepflow.nn.NN.forward","title":"<code>forward(inputs_dict: Dict[str, torch.Tensor]) -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Forward pass through the network.</p> <p>Parameters:</p> Name Type Description Default <code>inputs_dict</code> <code>Dict[str, Tensor]</code> <p>Dictionary containing input tensors (e.g., {'x': tensor, 'y': tensor})</p> required <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary mapping each configured output name to a prediction</p> <code>Dict[str, Tensor]</code> <p>tensor. Hard constraints are applied when configured.</p>"},{"location":"reference/#deepflow.nn.NN.apply_hard_constraints","title":"<code>apply_hard_constraints(bound_list: list)</code>","text":"<p>Configure output constraints from a list of boundary objects.</p> <p>Parameters:</p> Name Type Description Default <code>bound_list</code> <code>list</code> <p>Boundaries whose condition dictionaries may contain <code>HardConstraint</code> values.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If boundaries impose different constants for the same output field.</p>"},{"location":"reference/#deepflow.nn.NN.print_status","title":"<code>print_status()</code>","text":"<p>Print the most recently recorded training losses.</p>"},{"location":"reference/#deepflow.nn.NN.train_adam","title":"<code>train_adam(learning_rate: float, epochs: int, calc_loss: Callable, use_scheduler: Optional[bool] = False, print_every: int = 200, threshold_loss: Optional[float] = None, do_between_epochs: Optional[Callable] = None, compile_model: bool = False, max_grad_norm: Optional[float] = 1.0) -&gt; tuple[NN, NN]</code>","text":"<p>Train a copy of the model using the Adam optimizer.</p> <p>Parameters:</p> Name Type Description Default <code>learning_rate</code> <code>float</code> <p>Adam learning rate.</p> required <code>epochs</code> <code>int</code> <p>Maximum number of training epochs.</p> required <code>calc_loss</code> <code>Callable</code> <p>Callable returning a loss dictionary with <code>total_loss</code>.</p> required <code>use_scheduler</code> <code>Optional[bool]</code> <p>Whether to use a decaying step scheduler.</p> <code>False</code> <code>print_every</code> <code>int</code> <p>Print status at this many epochs.</p> <code>200</code> <code>threshold_loss</code> <code>Optional[float]</code> <p>Stop when the best loss falls below this value.</p> <code>None</code> <code>do_between_epochs</code> <code>Optional[Callable]</code> <p>Optional callback called as <code>callback(epoch, model)</code> after each epoch.</p> <code>None</code> <code>compile_model</code> <code>bool</code> <p>If <code>True</code>, wraps the model with <code>torch.compile</code> for kernel fusion and reduced overhead. Requires PyTorch 2.0+ and the Triton backend (Linux). The first epoch will be slower due to compilation; subsequent epochs benefit from fused kernels.</p> <code>False</code> <code>max_grad_norm</code> <code>Optional[float]</code> <p>Maximum global gradient norm used for clipping. Set to <code>None</code> to disable gradient clipping.</p> <code>1.0</code> <p>Returns:</p> Type Description <code>NN</code> <p>Tuple <code>(model, best_model)</code> containing the trained copy and a</p> <code>NN</code> <p>copy restored to its lowest recorded loss.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>print_every</code> or <code>max_grad_norm</code> is not positive.</p>"},{"location":"reference/#deepflow.nn.NN.train_lbfgs","title":"<code>train_lbfgs(epochs: int, calc_loss: Callable, print_every: int = 50, threshold_loss: Optional[float] = None, do_between_epochs: Optional[Callable] = None, compile_model: bool = False) -&gt; tuple[NN, NN]</code>","text":"<p>Train a copy of the model using the L-BFGS optimizer.</p> <p>Parameters:</p> Name Type Description Default <code>epochs</code> <code>int</code> <p>Maximum number of training epochs.</p> required <code>calc_loss</code> <code>Callable</code> <p>Callable returning a loss dictionary with <code>total_loss</code>.</p> required <code>print_every</code> <code>int</code> <p>Print status at this many epochs.</p> <code>50</code> <code>threshold_loss</code> <code>Optional[float]</code> <p>Stop when the loss falls below this value.</p> <code>None</code> <code>do_between_epochs</code> <code>Optional[Callable]</code> <p>Optional callback called as <code>callback(epoch, model)</code> after each epoch.</p> <code>None</code> <code>compile_model</code> <code>bool</code> <p>If <code>True</code>, wraps the model with <code>torch.compile</code> for kernel fusion and reduced overhead. Requires PyTorch 2.0+.</p> <code>False</code> <p>Returns:</p> Type Description <code>NN</code> <p>Tuple <code>(model, best_model)</code> containing the trained copy and a</p> <code>NN</code> <p>copy restored to its lowest recorded loss.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>print_every</code> is not positive.</p>"},{"location":"reference/#deepflow.nn.NN.save_as_pickle","title":"<code>save_as_pickle(file_name: str = 'model.pkl') -&gt; None</code>","text":"<p>Serialize the model to a pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>file_name</code> <code>str</code> <p>Output path. The <code>.pkl</code> suffix is added when absent.</p> <code>'model.pkl'</code> <p>Returns:</p> Type Description <code>None</code> <p>None.</p>"},{"location":"reference/#deepflow.nn.FNN","title":"<code>FNN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, hidden_layer: List[int] = [50, 50, 50, 50], activation: nn.Module = nn.Tanh(), weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>NN</code></p> <p>Fully connected feedforward neural network for PINN fields.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields.</p> <code>None</code> <code>hidden_layer</code> <code>List[int]</code> <p>Width of each hidden layer.</p> <code>[50, 50, 50, 50]</code> <code>activation</code> <code>Module</code> <p>PyTorch activation module inserted after hidden layers.</p> <code>Tanh()</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme passed to <code>NN</code>.</p> <code>'kaiming'</code> <p>Initialize and build the feedforward network.</p>"},{"location":"reference/#deepflow.nn.PINN","title":"<code>PINN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, width: int = 32, length: int = 4, activation: nn.Module = nn.Tanh(), weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>FNN</code></p> <p>Standard fully connected physics-informed neural network.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields.</p> <code>None</code> <code>width</code> <code>int</code> <p>Width of every hidden layer.</p> <code>32</code> <code>length</code> <code>int</code> <p>Number of hidden layers.</p> <code>4</code> <code>activation</code> <code>Module</code> <p>PyTorch activation module for hidden layers.</p> <code>Tanh()</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme.</p> <code>'kaiming'</code> <p>Initialize a PINN with equally wide hidden layers.</p>"},{"location":"reference/#deepflow.nn.RFFPINN","title":"<code>RFFPINN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, width: int = 32, length: int = 4, embed_dim: int = 256, alpha: float = 5.0, activation: nn.Module = nn.Tanh(), weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>FNN</code></p> <p>PINN with a fixed joint random Fourier feature input embedding.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields.</p> <code>None</code> <code>width</code> <code>int</code> <p>Width of each post-embedding hidden layer.</p> <code>32</code> <code>length</code> <code>int</code> <p>Number of post-embedding hidden layers.</p> <code>4</code> <code>embed_dim</code> <code>int</code> <p>Positive even dimension of the Fourier embedding.</p> <code>256</code> <code>alpha</code> <code>float</code> <p>Standard deviation scale of the sampled frequencies.</p> <code>5.0</code> <code>activation</code> <code>Module</code> <p>PyTorch activation module for hidden layers.</p> <code>Tanh()</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme.</p> <code>'kaiming'</code> <p>Initialize a PINN with a fixed random Fourier embedding.</p>"},{"location":"reference/#deepflow.nn.hard_constraint","title":"<code>hard_constraint(constant=0)</code>","text":"<p>Create a <code>HardConstraint</code> with a prescribed constant.</p> <p>Parameters:</p> Name Type Description Default <code>constant</code> <p>Value imposed on the constrained boundary or initial slice.</p> <code>0</code> <p>Returns:</p> Type Description <p>A <code>HardConstraint</code> instance.</p>"},{"location":"reference/#deepflow.nn.load_from_pickle","title":"<code>load_from_pickle(file_name: str) -&gt; NN</code>","text":"<p>Load a model serialized by <code>NN.save_as_pickle</code>.</p> <p>Parameters:</p> Name Type Description Default <code>file_name</code> <code>str</code> <p>Pickle path. The <code>.pkl</code> suffix is added when absent.</p> required <p>Returns:</p> Type Description <code>NN</code> <p>The deserialized model instance.</p>"},{"location":"reference/#evaluation","title":"Evaluation","text":"<p>Model evaluation, derived fields, and structured plotting results.</p>"},{"location":"reference/#deepflow.evaluation.Evaluator","title":"<code>Evaluator(pinns_model: PINN, geometry: Area | Bound | CustomData)</code>","text":"<p>               Bases: <code>Visualizer</code></p> <p>Evaluate a PINN on one geometry and prepare plotting data.</p> <p>Parameters:</p> Name Type Description Default <code>pinns_model</code> <code>PINN</code> <p>PINN model used to calculate predictions and residuals.</p> required <code>geometry</code> <code>Area | Bound | CustomData</code> <p>Sampled <code>Area</code>, <code>Bound</code>, or <code>CustomData</code> object.</p> required <p>Attributes:</p> Name Type Description <code>data_dict</code> <code>Dict[str, Any]</code> <p>Materialized NumPy fields, coordinates, residuals, and loss history.</p> <code>expr</code> <code>_ExpressionNamespace</code> <p>Mapping-like namespace for persistent derived fields.</p> Notes <p>Sampling and time changes refresh <code>data_dict</code> automatically. Fields assigned through <code>expr</code> are recomputed after each refresh.</p> <p>Initialize an evaluator for a model and geometry.</p> <p>Parameters:</p> Name Type Description Default <code>pinns_model</code> <code>PINN</code> <p>The physics-informed neural network model.</p> required <code>geometry</code> <code>Area | Bound | CustomData</code> <p>The geometric domain or custom data to evaluate.</p> required"},{"location":"reference/#deepflow.evaluation.Evaluator.expr","title":"<code>expr: _ExpressionNamespace</code>  <code>property</code>","text":"<p>Namespace for persistent lazy data-field expressions.</p> <p>Returns:</p> Type Description <code>_ExpressionNamespace</code> <p>Mapping-like object supporting expressions such as</p> <code>_ExpressionNamespace</code> <p><code>evaluator.expr[\"speed\"] = (evaluator.expr[\"u\"] ** 2) ** 0.5</code>.</p>"},{"location":"reference/#deepflow.evaluation.Evaluator.sampling_line","title":"<code>sampling_line(n_points: int, scheme: str = 'uniform') -&gt; None</code>","text":"<p>Sample a boundary and refresh evaluated fields.</p> <p>Parameters:</p> Name Type Description Default <code>n_points</code> <code>int</code> <p>Number of boundary points.</p> required <code>scheme</code> <code>str</code> <p><code>\"uniform\"</code>, <code>\"random\"</code>, or <code>\"lhs\"</code>.</p> <code>'uniform'</code>"},{"location":"reference/#deepflow.evaluation.Evaluator.sampling_area","title":"<code>sampling_area(res_list: List[int], scheme: str = 'uniform') -&gt; None</code>","text":"<p>Sample an area and refresh evaluated fields.</p> <p>Parameters:</p> Name Type Description Default <code>res_list</code> <code>List[int]</code> <p>Area resolution as a scalar or <code>[nx, ny]</code> pair.</p> required <code>scheme</code> <code>str</code> <p><code>\"uniform\"</code>, <code>\"random\"</code>, or <code>\"lhs\"</code>.</p> <code>'uniform'</code>"},{"location":"reference/#deepflow.evaluation.Evaluator.define_time","title":"<code>define_time(range_t: Union[float, int, List[float]], sampling_scheme: str = 'uniform', expo_scaling: Optional[bool] = None) -&gt; None</code>","text":"<p>Define time coordinates and refresh evaluated fields.</p> <p>Parameters:</p> Name Type Description Default <code>range_t</code> <code>Union[float, int, List[float]]</code> <p>Time interval or fixed time value.</p> required <code>sampling_scheme</code> <code>str</code> <p>Time sampling scheme.</p> <code>'uniform'</code> <code>expo_scaling</code> <code>Optional[bool]</code> <p>Whether to apply exponential time scaling.</p> <code>None</code>"},{"location":"reference/#deepflow.evaluation.Evaluator.postprocess","title":"<code>postprocess() -&gt; None</code>","text":"<p>Recompute predictions, residuals, coordinates, and derived fields.</p> <p>Returns:</p> Type Description <code>None</code> <p>None. <code>data_dict</code> is replaced with NumPy-compatible values.</p>"},{"location":"reference/#deepflow.evaluation.Evaluator.plot_animate","title":"<code>plot_animate(color_axis: str, x_axis: str = 'x', y_axis: str = 'y', cmap='viridis', range_t=None, dt=None, frame_interval=10, plot_type: str = 'scatter', s=6, color_range: list = None) -&gt; Any</code>","text":"<p>Create and display an animation over time for a field.</p> <p>Parameters:</p> Name Type Description Default <code>color_axis</code> <code>str</code> <p>Field to animate.</p> required <code>x_axis</code> <code>str</code> <p>x-coordinate field.</p> <code>'x'</code> <code>y_axis</code> <code>str</code> <p>y-coordinate field.</p> <code>'y'</code> <code>cmap</code> <p>Matplotlib colormap name.</p> <code>'viridis'</code> <code>range_t</code> <p>Two-element animation time interval.</p> <code>None</code> <code>dt</code> <p>Time increment between frames.</p> <code>None</code> <code>frame_interval</code> <p>Delay between frames in milliseconds.</p> <code>10</code> <code>plot_type</code> <code>str</code> <p><code>\"scatter\"</code>, <code>\"tripcolor\"</code>, or <code>\"contourf\"</code>.</p> <code>'scatter'</code> <code>s</code> <p>Scatter marker size.</p> <code>6</code> <code>color_range</code> <code>list</code> <p>Optional <code>[min, max]</code> color limits.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>The created Matplotlib animation object.</p>"},{"location":"reference/#deepflow.evaluation.ReferenceEvaluator","title":"<code>ReferenceEvaluator(reference_solution, geometry: Area | Bound | CustomData)</code>","text":"<p>               Bases: <code>Evaluator</code></p> <p>Evaluate a solved <code>ReferenceSolution</code> on one geometry.</p> <p>Reference evaluators deliberately do not calculate PINN residuals or copy model training history.  They retain the FEM solution and re-query it whenever the geometry is sampled again or its time coordinates change.</p> <p>Parameters:</p> Name Type Description Default <code>reference_solution</code> <p>Solved <code>ReferenceSolution</code> queried by this evaluator.</p> required <code>geometry</code> <code>Area | Bound | CustomData</code> <p>Geometry whose coordinates are used for FEM queries.</p> required <p>Initialize an evaluator backed by a reference solution.</p>"},{"location":"reference/#deepflow.evaluation.ReferenceEvaluator.define_time","title":"<code>define_time(range_t: Union[float, int, List[float]], sampling_scheme: str = 'uniform', expo_scaling: Optional[bool] = None) -&gt; None</code>","text":"<p>Update time coordinates and re-query the FEM fields.</p> <p>Parameters:</p> Name Type Description Default <code>range_t</code> <code>Union[float, int, List[float]]</code> <p>Time interval or fixed time value.</p> required <code>sampling_scheme</code> <code>str</code> <p>Time sampling scheme.</p> <code>'uniform'</code> <code>expo_scaling</code> <code>Optional[bool]</code> <p>Whether to apply exponential time scaling.</p> <code>None</code>"},{"location":"reference/#deepflow.evaluation.ReferenceEvaluator.postprocess","title":"<code>postprocess() -&gt; None</code>","text":"<p>Re-query the reference fields and refresh derived fields.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator","title":"<code>GroupEvaluator(pinns_model: PINN, domain)</code>","text":"<p>Coordinates geometry-specific <code>Evaluator</code> instances for a domain.</p> <p>Results remain structured per geometry.  The group does not merge data dictionaries because different geometries can represent different physics types and therefore expose different residual fields.</p> <p>Parameters:</p> Name Type Description Default <code>pinns_model</code> <code>PINN</code> <p>PINN model shared by child evaluators.</p> required <code>domain</code> <p>Domain containing the sampled geometries.</p> required <p>Attributes:</p> Name Type Description <code>bound_list</code> <p>Evaluators aligned with unique domain boundaries.</p> <code>area_list</code> <p>Evaluators aligned with unique domain areas.</p> <p>Build one evaluator for each unique domain geometry.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.postprocess","title":"<code>postprocess() -&gt; None</code>","text":"<p>Refresh all child evaluators after sampling or model changes.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.refresh","title":"<code>refresh() -&gt; None</code>","text":"<p>Alias for <code>postprocess</code>.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.sampling_line","title":"<code>sampling_line(n_points, scheme: str = 'uniform') -&gt; None</code>","text":"<p>Sample and refresh every boundary child.</p> <p>Parameters:</p> Name Type Description Default <code>n_points</code> <p>Scalar count or one count per boundary child.</p> required <code>scheme</code> <code>str</code> <p>Boundary sampling scheme.</p> <code>'uniform'</code>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.sampling_area","title":"<code>sampling_area(res_list, scheme: str = 'uniform') -&gt; None</code>","text":"<p>Sample and refresh every Area child.</p> <p><code>CustomData</code> entries are intentionally left unchanged because they represent fixed user-provided coordinates rather than a sampler.</p> <p>Parameters:</p> Name Type Description Default <code>res_list</code> <p>Scalar, one resolution per area, or <code>[nx, ny]</code> for a single area.</p> required <code>scheme</code> <code>str</code> <p>Area sampling scheme.</p> <code>'uniform'</code>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.define_time","title":"<code>define_time(range_t: Union[float, int, List[float]], sampling_scheme: str = 'uniform', expo_scaling: Optional[bool] = None) -&gt; None</code>","text":"<p>Define and refresh time coordinates for every child geometry.</p> <p>Parameters:</p> Name Type Description Default <code>range_t</code> <code>Union[float, int, List[float]]</code> <p>Time interval or fixed time value.</p> required <code>sampling_scheme</code> <code>str</code> <p>Time sampling scheme.</p> <code>'uniform'</code> <code>expo_scaling</code> <code>Optional[bool]</code> <p>Whether to apply exponential time scaling.</p> <code>None</code>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot","title":"<code>plot(*args, geometry, **kwargs)</code>","text":"<p>Delegate a general plot to a geometry-specific evaluator.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional plotting arguments.</p> <code>()</code> <code>geometry</code> <p>Geometry whose child evaluator should plot.</p> required <code>**kwargs</code> <p>Keyword plotting arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>The figure returned by the child evaluator.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_color","title":"<code>plot_color(*args, geometry=None, **kwargs)</code>","text":"<p>Plot one geometry or aggregate compatible geometries.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments accepted by <code>Visualizer.plot_color</code>.</p> <code>()</code> <code>geometry</code> <p>Optional geometry for a child-specific plot. If omitted, compatible child data is concatenated temporarily.</p> <code>None</code> <code>**kwargs</code> <p>Keyword arguments accepted by the color plot.</p> <code>{}</code> <p>Returns:</p> Type Description <p>The generated figure or <code>(figure, axes)</code> result.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_contour","title":"<code>plot_contour(*args, geometry, **kwargs)</code>","text":"<p>Delegate a contour plot to a selected geometry evaluator.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_streamline","title":"<code>plot_streamline(*args, geometry, **kwargs)</code>","text":"<p>Delegate a streamline plot to a selected geometry evaluator.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_distribution","title":"<code>plot_distribution(*args, geometry, **kwargs)</code>","text":"<p>Delegate a distribution plot to a selected geometry evaluator.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_loss_curve","title":"<code>plot_loss_curve(*args, geometry, **kwargs)</code>","text":"<p>Delegate a loss-curve plot to a selected geometry evaluator.</p>"},{"location":"reference/#deepflow.evaluation.GroupEvaluator.plot_animate","title":"<code>plot_animate(*args, geometry, **kwargs)</code>","text":"<p>Delegate an animation to a selected geometry evaluator.</p>"},{"location":"reference/#deepflow.evaluation.ReferenceGroupEvaluator","title":"<code>ReferenceGroupEvaluator(reference_solution, domain)</code>","text":"<p>               Bases: <code>GroupEvaluator</code></p> <p>GroupEvaluator whose children query a solved FEM reference solution.</p> <p>This is the <code>solve_fem</code> result: a real <code>GroupEvaluator</code> where each per-geometry <code>ReferenceEvaluator</code> re-queries the FEM fields on its geometry.  The underlying <code>ReferenceSolution</code> remains available as <code>.reference_solution</code> for arbitrary point queries and export.</p> <p>Parameters:</p> Name Type Description Default <code>reference_solution</code> <p>Solved reference solution used by child evaluators.</p> required <code>domain</code> <p>Domain containing the geometries to evaluate.</p> required <p>Build reference evaluators for every unique domain geometry.</p>"},{"location":"reference/#visualization","title":"Visualization","text":"<p>Plotting helpers for evaluated DeepFlow fields.</p>"},{"location":"reference/#deepflow.visualization.Visualizer","title":"<code>Visualizer(data_dict: Dict[str, np.ndarray])</code>","text":"<p>Create plots from a mapping of field names to NumPy arrays.</p> <p>Parameters:</p> Name Type Description Default <code>data_dict</code> <code>Dict[str, ndarray]</code> <p>Evaluated fields and coordinates. Plot methods expect the requested field keys and usually <code>x</code> and <code>y</code> coordinates.</p> required <p>Initialize a visualizer from evaluated data.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot_color","title":"<code>plot_color(color_axis: str, x_axis: str = 'x', y_axis: str = 'y', cmap='viridis', s: Union[int, float] = 2, return_ax: bool = False) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Create a scatter plot colored by a field.</p> <p>Parameters:</p> Name Type Description Default <code>color_axis</code> <code>str</code> <p>Field used for point colors.</p> required <code>x_axis</code> <code>str</code> <p>Field used for horizontal coordinates.</p> <code>'x'</code> <code>y_axis</code> <code>str</code> <p>Field used for vertical coordinates.</p> <code>'y'</code> <code>cmap</code> <p>Matplotlib colormap name.</p> <code>'viridis'</code> <code>s</code> <code>Union[int, float]</code> <p>Marker size.</p> <code>2</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot","title":"<code>plot(z_axis: str = None, x_axis: str = 'x', y_axis: str = 'y', return_ax: bool = False, color=None) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Create a line plot or a three-dimensional scatter plot.</p> <p>Parameters:</p> Name Type Description Default <code>z_axis</code> <code>str</code> <p>Optional field for a 3-D scatter plot. If omitted, plot <code>y_axis</code> against <code>x_axis</code> as a line.</p> <code>None</code> <code>x_axis</code> <code>str</code> <p>Field used for horizontal coordinates.</p> <code>'x'</code> <code>y_axis</code> <code>str</code> <p>Field used for vertical coordinates.</p> <code>'y'</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <code>color</code> <p>Optional line color or colormap name.</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot_distribution","title":"<code>plot_distribution(key: str, bins: Union[str, int] = 'fd', return_ax: bool = False) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Plot a histogram for a field.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Field to plot.</p> required <code>bins</code> <code>Union[str, int]</code> <p>Number of bins or a NumPy binning strategy.</p> <code>'fd'</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot_loss_curve","title":"<code>plot_loss_curve(log_scale: bool = True, start: int = 0, end: Optional[int] = None, keys: Tuple[str, ...] = ('total_loss', 'bc_loss', 'pde_loss'), return_ax: bool = False) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Plot recorded losses over training iterations.</p> <p>Parameters:</p> Name Type Description Default <code>log_scale</code> <code>bool</code> <p>Use a logarithmic y-axis.</p> <code>True</code> <code>start</code> <code>int</code> <p>First history index to display.</p> <code>0</code> <code>end</code> <code>Optional[int]</code> <p>Exclusive final history index.</p> <code>None</code> <code>keys</code> <code>Tuple[str, ...]</code> <p>Loss-history keys to plot when present.</p> <code>('total_loss', 'bc_loss', 'pde_loss')</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot_contour","title":"<code>plot_contour(color_axis: str, x_axis: str = 'x', y_axis: str = 'y', cmap='jet', levels=50, return_ax: bool = False) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Create a filled contour plot for an interpolated field.</p> <p>Parameters:</p> Name Type Description Default <code>color_axis</code> <code>str</code> <p>Field to interpolate and plot.</p> required <code>x_axis</code> <code>str</code> <p>x-coordinate field.</p> <code>'x'</code> <code>y_axis</code> <code>str</code> <p>y-coordinate field.</p> <code>'y'</code> <code>cmap</code> <p>Matplotlib colormap name.</p> <code>'jet'</code> <code>levels</code> <p>Number of contour levels.</p> <code>50</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#deepflow.visualization.Visualizer.plot_streamline","title":"<code>plot_streamline(u: str, v: str, x_axis: str = 'x', y_axis: str = 'y', cmap='viridis', levels=100, return_ax: bool = False) -&gt; Union[plt.Figure, Tuple[plt.Figure, object]]</code>","text":"<p>Create a streamline plot for two interpolated vector components.</p> <p>Parameters:</p> Name Type Description Default <code>u</code> <code>str</code> <p>Field containing the x component.</p> required <code>v</code> <code>str</code> <p>Field containing the y component.</p> required <code>x_axis</code> <code>str</code> <p>x-coordinate field.</p> <code>'x'</code> <code>y_axis</code> <code>str</code> <p>y-coordinate field.</p> <code>'y'</code> <code>cmap</code> <p>Matplotlib colormap name.</p> <code>'viridis'</code> <code>levels</code> <p>Streamline density or plotting level configuration.</p> <code>100</code> <code>return_ax</code> <code>bool</code> <p>Return <code>(figure, axes)</code> instead of only the figure.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[Figure, Tuple[Figure, object]]</code> <p>A figure, or a <code>(figure, axes)</code> tuple when <code>return_ax</code> is true.</p>"},{"location":"reference/#utilities","title":"Utilities","text":"<p>Global configuration, reproducibility, and autograd utilities.</p>"},{"location":"reference/#deepflow.utility.latin_hypercube_sampling","title":"<code>latin_hypercube_sampling(n_samples: int, n_dimensions: int, lower_lim: list, upper_lim: list, seed: Optional[int] = None) -&gt; torch.Tensor</code>","text":"<p>Generates Latin Hypercube Samples scaled to [lower_lim, upper_lim].</p> Reproducibility <p>Pass an explicit <code>seed</code>, or set one globally via <code>manual_seed</code>. If neither is provided, SciPy's default (<code>None</code>) is used and results will vary between runs.</p> <p>Parameters:</p> Name Type Description Default <code>n_samples</code> <code>int</code> <p>Number of samples.</p> required <code>n_dimensions</code> <code>int</code> <p>Number of dimensions.</p> required <code>lower_lim</code> <code>list</code> <p>Lower bound for each dimension.</p> required <code>upper_lim</code> <code>list</code> <p>Upper bound for each dimension.</p> required <code>seed</code> <code>Optional[int]</code> <p>Optional per-call random seed.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>A tensor of shape <code>(n_samples, n_dimensions)</code>.</p>"},{"location":"reference/#deepflow.utility.get_device","title":"<code>get_device()</code>","text":"<p>Return the configured PyTorch device name.</p>"},{"location":"reference/#deepflow.utility.get_dtype","title":"<code>get_dtype() -&gt; torch.dtype</code>","text":"<p>Return the current global floating-point dtype used by deepflow.</p>"},{"location":"reference/#deepflow.utility.set_dtype","title":"<code>set_dtype(value: torch.dtype) -&gt; None</code>","text":"<p>Set the global floating-point dtype used by deepflow.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>dtype</code> <p><code>torch.float32</code> or <code>torch.float64</code>. Also accepts the aliases <code>torch.float</code> and <code>torch.double</code>.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>value</code> is not a supported floating-point dtype.</p> <p>Returns:</p> Type Description <code>None</code> <p>None. The global default dtype is updated in place.</p>"},{"location":"reference/#deepflow.utility.manual_seed","title":"<code>manual_seed(seed: int, deterministic: bool = False)</code>","text":"<p>Set all random seeds for reproducible training runs.</p> <p>Seeds Python's <code>random</code>, NumPy, PyTorch (CPU &amp; GPU), and configures cuDNN for determinism when CUDA is available.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <code>int</code> <p>Integer seed passed to all RNGs.</p> required <code>deterministic</code> <code>bool</code> <p>If <code>True</code>, enables PyTorch's deterministic mode via <code>torch.use_deterministic_algorithms(True)</code> (may impact performance).</p> <code>False</code> <p>Returns:</p> Type Description <p>None.</p>"},{"location":"reference/#deepflow.utility.calc_grad","title":"<code>calc_grad(y: torch.Tensor, x: torch.Tensor) -&gt; torch.Tensor</code>","text":"<p>Calculates the gradient of tensor y with respect to tensor x.</p> <p>If <code>y</code> does not depend on <code>x</code>, return a zero tensor with the same shape, device, and dtype as <code>x</code>.</p> <p>Parameters:</p> Name Type Description Default <code>y</code> <code>Tensor</code> <p>Tensor whose gradient is requested.</p> required <code>x</code> <code>Tensor</code> <p>Tensor with respect to which the gradient is taken.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Gradient tensor with the same shape as <code>x</code>.</p>"},{"location":"reference/#deepflow.utility.calc_grads","title":"<code>calc_grads(y: torch.Tensor, x_list: Union[Tuple[torch.Tensor, ...], List[torch.Tensor]]) -&gt; Tuple[torch.Tensor, ...]</code>","text":"<p>Calculate gradients of one tensor with respect to several tensors.</p> <p>Parameters:</p> Name Type Description Default <code>y</code> <code>Tensor</code> <p>Tensor whose gradients are requested.</p> required <code>x_list</code> <code>Union[Tuple[Tensor, ...], List[Tensor]]</code> <p>Coordinate tensors used as differentiation inputs.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Tuple of gradients aligned with <code>x_list</code>. Unused inputs receive zero</p> <code>...</code> <p>tensors.</p>"},{"location":"reference/#deepflow.utility.to_require_grad","title":"<code>to_require_grad(*tensors: torch.Tensor) -&gt; Union[torch.Tensor, Tuple[torch.Tensor, ...]]</code>","text":"<p>Clone tensors and enable gradients for PINN training.</p> <p>Parameters:</p> Name Type Description Default <code>*tensors</code> <code>Tensor</code> <p>Tensors to detach, clone, and mark differentiable.</p> <code>()</code> <p>Returns:</p> Type Description <code>Union[Tensor, Tuple[Tensor, ...]]</code> <p>One tensor for a single input, otherwise a tuple of tensors.</p>"},{"location":"reference/#deepflow.utility.torch_to_numpy","title":"<code>torch_to_numpy(*tensors: torch.Tensor) -&gt; Union[float, Tuple]</code>","text":"<p>Convert CPU or GPU tensors to NumPy arrays.</p> <p>Parameters:</p> Name Type Description Default <code>*tensors</code> <code>Tensor</code> <p>Tensors to detach and move to CPU.</p> <code>()</code> <p>Returns:</p> Type Description <code>Union[float, Tuple]</code> <p>One NumPy array for a single input, otherwise a tuple of arrays.</p>"},{"location":"reference/#fem-backend","title":"FEM Backend","text":"<p>FEM evaluation orchestration for <code>ProblemDomain</code>.</p>"},{"location":"reference/#deepflow.fem.solve_fem","title":"<code>solve_fem(domain: ProblemDomain, mesh_size=0.05, boundary_resolution=128, time_step=None, tolerance=1e-08, max_iterations=200) -&gt; ReferenceGroupEvaluator</code>","text":"<p>Solve a domain with the optional NGSolve FEM backend.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <code>ProblemDomain</code> <p>Domain containing exactly one area with an attached PDE.</p> required <code>mesh_size</code> <p>Target FEM mesh size, or <code>None</code> to infer it.</p> <code>0.05</code> <code>boundary_resolution</code> <p>Number of samples used to construct mesh boundary curves.</p> <code>128</code> <code>time_step</code> <p>Time spacing for transient reference snapshots.</p> <code>None</code> <code>tolerance</code> <p>Iterative solver convergence tolerance.</p> <code>1e-08</code> <code>max_iterations</code> <p>Maximum number of solver iterations.</p> <code>200</code> <p>Returns:</p> Type Description <code>ReferenceGroupEvaluator</code> <p>A <code>deepflow.evaluation.ReferenceGroupEvaluator</code>.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If NGSolve/Netgen is not installed.</p> <code>ValueError</code> <p>If the domain or solver configuration is invalid.</p>"},{"location":"reference/#reference-solutions","title":"Reference Solutions","text":"<p>Optional NGSolve reference solutions for DeepFlow problems.</p> <p>The module itself is dependency-light.  NGSolve is imported only when <code>ReferenceSolver.solve</code> is called.</p>"},{"location":"reference/#deepflow.reference.ReferenceConfigurationError","title":"<code>ReferenceConfigurationError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised for a valid DeepFlow object that is unsupported in v1.</p>"},{"location":"reference/#deepflow.reference.ReferenceGeometryError","title":"<code>ReferenceGeometryError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when a DeepFlow geometry cannot be meshed by the reference backend.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution","title":"<code>ReferenceSolution(*, area, mesh, fields: Optional[Mapping[str, object]] = None, snapshots: Optional[Sequence[Mapping[str, object]]] = None, times: Optional[Sequence[float]] = None, metadata: Optional[Mapping[str, object]] = None, field_evaluator: Optional[Callable[[object, np.ndarray, np.ndarray], np.ndarray]] = None, time_from_y: bool = False)</code>","text":"<p>A solved field collection backed by NGSolve grid functions.</p> <p><code>ReferenceSolution</code> deliberately keeps the FEM objects alive.  Queries therefore evaluate the same fields that were produced by the solver, rather than interpolating a second sampled representation.</p> <p>Attributes:</p> Name Type Description <code>metadata</code> <code>dict</code> <p>JSON-compatible solver metadata.</p> <code>fields</code> <code>Tuple[str, ...]</code> <p>Names of fields available to <code>evaluate</code>.</p> <code>times</code> <code>Optional[ndarray]</code> <p>Stored transient snapshot times, if any.</p> <p>Initialize a solved steady or transient field collection.</p> <p>Parameters:</p> Name Type Description Default <code>area</code> <p>PDE area used to validate query points.</p> required <code>mesh</code> <p>FEM mesh retained by the field evaluator.</p> required <code>fields</code> <code>Optional[Mapping[str, object]]</code> <p>Steady field objects keyed by field name.</p> <code>None</code> <code>snapshots</code> <code>Optional[Sequence[Mapping[str, object]]]</code> <p>Transient field mappings ordered by <code>times</code>.</p> <code>None</code> <code>times</code> <code>Optional[Sequence[float]]</code> <p>Strictly increasing transient snapshot times.</p> <code>None</code> <code>metadata</code> <code>Optional[Mapping[str, object]]</code> <p>Solver metadata retained for inspection and export.</p> <code>None</code> <code>field_evaluator</code> <code>Optional[Callable[[object, ndarray, ndarray], ndarray]]</code> <p>Optional callable overriding point evaluation.</p> <code>None</code> <code>time_from_y</code> <code>bool</code> <p>Whether the supplied y coordinate represents time.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If fields/snapshots or transient times are incomplete or inconsistent.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.metadata","title":"<code>metadata: dict</code>  <code>property</code>","text":"<p>Solver metadata, including mesh and convergence information.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.fields","title":"<code>fields: Tuple[str, ...]</code>  <code>property</code>","text":"<p>Names of fields accepted by <code>evaluate</code>.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.times","title":"<code>times: Optional[np.ndarray]</code>  <code>property</code>","text":"<p>Stored transient snapshot times, or <code>None</code> for steady fields.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.is_transient","title":"<code>is_transient: bool</code>  <code>property</code>","text":"<p>Whether this solution contains transient snapshots.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.time_from_y","title":"<code>time_from_y: bool</code>  <code>property</code>","text":"<p>Whether the existing <code>y</code> coordinate carries transient time.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.evaluate","title":"<code>evaluate(x, y, t=None, fields=None) -&gt; Dict[str, np.ndarray]</code>","text":"<p>Evaluate selected fields at broadcastable point coordinates.</p> <p>For transient solutions, values are linearly interpolated between the stored solver snapshots. A transient query must provide <code>t</code> unless <code>time_from_y</code> is enabled, in which case <code>y</code> supplies time. Steady fields ignore a supplied time coordinate.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <p>x coordinates, broadcastable with <code>y</code> and optional <code>t</code>.</p> required <code>y</code> <p>y coordinates, broadcastable with <code>x</code> and optional <code>t</code>.</p> required <code>t</code> <p>Optional time coordinates for transient solutions, unless <code>time_from_y</code> is enabled.</p> <code>None</code> <code>fields</code> <p>Optional field name or iterable of field names. <code>None</code> selects every declared field.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dictionary mapping selected field names to NumPy arrays matching</p> <code>Dict[str, ndarray]</code> <p>the broadcast query shape.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If a requested field is unknown.</p> <code>ValueError</code> <p>If points lie outside the PDE area, required transient time is missing, or times are outside the stored interval.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolution.export_npz","title":"<code>export_npz(path, x, y, t=None, fields=None) -&gt; None</code>","text":"<p>Export queried values and metadata to a compressed <code>.npz</code> file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>Output path.</p> required <code>x</code> <p>x coordinates accepted by <code>evaluate</code>.</p> required <code>y</code> <p>y coordinates accepted by <code>evaluate</code>.</p> required <code>t</code> <p>Optional transient time coordinates.</p> <code>None</code> <code>fields</code> <p>Optional field name or iterable of field names.</p> <code>None</code> <p>Returns:</p> Type Description <code>None</code> <p>None.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolver","title":"<code>ReferenceSolver(mesh_size=None, boundary_resolution=128, time_step=None, tolerance=1e-08, max_iterations=200)</code>","text":"<p>Solve DeepFlow's built-in PDEs with optional NGSolve/Netgen FEM.</p> <p>Parameters:</p> Name Type Description Default <code>mesh_size</code> <p>Target mesh size, or <code>None</code> to infer it from the PDE area.</p> <code>None</code> <code>boundary_resolution</code> <p>Boundary sampling resolution used during meshing.</p> <code>128</code> <code>time_step</code> <p>Snapshot spacing for transient problems.</p> <code>None</code> <code>tolerance</code> <p>Iterative solver convergence tolerance.</p> <code>1e-08</code> <code>max_iterations</code> <p>Maximum number of solver iterations.</p> <code>200</code> <p>Validate and store reference-solver configuration.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolver.clear_cache","title":"<code>clear_cache()</code>","text":"<p>Retain the historical cache API; solutions are not solver-cached.</p> <p>Returns:</p> Type Description <p>None.</p>"},{"location":"reference/#deepflow.reference.ReferenceSolver.solve","title":"<code>solve(domain) -&gt; ReferenceSolution</code>","text":"<p>Solve the PDE attached to one <code>ProblemDomain</code> area.</p> <p>Parameters:</p> Name Type Description Default <code>domain</code> <p>Domain containing exactly one area with a supported PDE.</p> required <p>Returns:</p> Type Description <code>ReferenceSolution</code> <p>A <code>ReferenceSolution</code> retaining the solved FEM fields.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If the optional NGSolve/Netgen backend is unavailable.</p> <code>ReferenceConfigurationError</code> <p>If geometry, conditions, or time configuration is unsupported.</p> <code>UnsupportedReferencePDE</code> <p>If the attached PDE has no FEM backend.</p>"},{"location":"reference/#deepflow.reference.UnsupportedReferencePDE","title":"<code>UnsupportedReferencePDE</code>","text":"<p>               Bases: <code>NotImplementedError</code></p> <p>Raised when a PDE has no reference-backend implementation.</p>"},{"location":"reference/#quantum-models","title":"Quantum Models","text":"<p>Optional quantum-enhanced PINN architectures.</p>"},{"location":"reference/#deepflow.qnn.QCPINN","title":"<code>QCPINN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, nqubits: Optional[int] = 4, q_layer_type: str = 'cascade', q_layer_iterations: int = 1, hidden_layer_pre: Optional[List[int]] = None, hidden_layer_post: Optional[List[int]] = None, activation: nn.Module = nn.Tanh(), weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>NN</code></p> <p>Quantum-circuit PINN with a configurable trainable circuit block.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields.</p> <code>None</code> <code>nqubits</code> <code>Optional[int]</code> <p>Number of qubits in the circuit.</p> <code>4</code> <code>q_layer_type</code> <code>str</code> <p>Circuit topology: <code>\"cascade\"</code> or <code>\"hea\"</code>.</p> <code>'cascade'</code> <code>q_layer_iterations</code> <code>int</code> <p>Number of repeated quantum layers.</p> <code>1</code> <code>hidden_layer_pre</code> <code>Optional[List[int]]</code> <p>Classical layer widths before the quantum layer.</p> <code>None</code> <code>hidden_layer_post</code> <code>Optional[List[int]]</code> <p>Classical layer widths after the quantum layer.</p> <code>None</code> <code>activation</code> <code>Module</code> <p>PyTorch activation module for classical layers.</p> <code>Tanh()</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme inherited from <code>NN</code>.</p> <code>'kaiming'</code> <p>Initialize and build the quantum-circuit PINN.</p>"},{"location":"reference/#deepflow.qnn.QPINN","title":"<code>QPINN(input_vars: Optional[List[str]] = None, output_vars: Optional[List[str]] = None, nqubits: Optional[int] = 4, q_depth: int = 4, feature_map: str = 'simple', ansatz: str = 'hea', cost_func: str = 'simple', hidden_layer_pre: Optional[List[int]] = None, hidden_layer_post: Optional[List[int]] = None, activation: nn.Module = nn.Tanh(), weight_init: Union[str, Callable, None] = 'kaiming')</code>","text":"<p>               Bases: <code>NN</code></p> <p>Quantum PINN with configurable feature map and ansatz layers.</p> <p>Parameters:</p> Name Type Description Default <code>input_vars</code> <code>Optional[List[str]]</code> <p>Names of coordinate inputs.</p> <code>None</code> <code>output_vars</code> <code>Optional[List[str]]</code> <p>Names of predicted fields.</p> <code>None</code> <code>nqubits</code> <code>Optional[int]</code> <p>Number of qubits in the circuit.</p> <code>4</code> <code>q_depth</code> <code>int</code> <p>Number of trainable quantum layers.</p> <code>4</code> <code>feature_map</code> <code>str</code> <p>Feature map: <code>\"simple\"</code>, <code>\"product\"</code>, or <code>\"chebyshev\"</code>.</p> <code>'simple'</code> <code>ansatz</code> <code>str</code> <p>Trainable circuit block: <code>\"simple\"</code>, <code>\"hea\"</code>, or <code>\"cascade\"</code>.</p> <code>'hea'</code> <code>cost_func</code> <code>str</code> <p>Measurement block. Only <code>\"simple\"</code> is currently supported.</p> <code>'simple'</code> <code>hidden_layer_pre</code> <code>Optional[List[int]]</code> <p>Classical layer widths before the quantum layer.</p> <code>None</code> <code>hidden_layer_post</code> <code>Optional[List[int]]</code> <p>Classical layer widths after the quantum layer.</p> <code>None</code> <code>activation</code> <code>Module</code> <p>PyTorch activation module for classical layers.</p> <code>Tanh()</code> <code>weight_init</code> <code>Union[str, Callable, None]</code> <p>Weight initialization scheme inherited from <code>NN</code>.</p> <code>'kaiming'</code> <p>Initialize and build the quantum PINN.</p>"}]}